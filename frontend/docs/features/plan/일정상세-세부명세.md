# 일정 상세 세부 명세

> 상위 공통명세: `docs/features/plan/공통명세.md` (목록 · 직접 만들기)
> **화면 정본**: `혼디가개 여행 일정.dc.html` 01(상세 — 모바일 · DRAFT) · 02(상세 — 데스크톱 · CONFIRMED) · 06 ③(판정만 실패)
> 작성일: 2026-08-31
> **계약 확인 근거**: backend plan-service **소스 실측** — `PlanWebController` ·
> `PlanDetailResponse` · `PlanItemDetail` · `PlanUpdateRequest` · `PlanWeatherResponse` ·
> `PlanDayWeatherItem` · `PlanDailyWeatherItem` · `PlanCommandProcessor` · `PlanErrorCode`
> (백엔드 `origin/develop` `8e44ddc` 기준, 2026-08-31).
> 게이트웨이 미기동이라 Swagger 실호출은 하지 못했다 — [#67](https://github.com/8llow8llowMe/hondigagae/issues/67) 에서 대조한다.

## 이 문서의 범위

아트보드 01·02·03 을 한 PR 에 담으면 30파일을 크게 넘는다. **명세는 한 번에 쓰고 구현은 셋으로 나눈다.**

| 이슈  | 범위                                                       | 문서                      |
| ----- | ---------------------------------------------------------- | ------------------------- |
| **A** | 상세 읽기 · 일자별 판정 · 확정하기 · 이름·예산 수정 · 삭제 | **이 문서**               |
| **B** | 일자 편집모드 (순서 · 삭제 보류 · 저장 1회)                | `일자편집-세부명세.md` E~ |
| **C** | 장소 추가 · 실내 대안 담기                                 | `일자편집-세부명세.md` F~ |

A 를 먼저 머지하는 이유: 목록 행과 홈 `upcoming-plan-row.tsx` 가 **이미 `/plans/{planId}` 로
링크하고 있어 지금 404 다.** 그 404 는 이 화면이 만든 것이 아니라 [#51](https://github.com/8llow8llowMe/hondigagae/issues/51)·[#75](https://github.com/8llow8llowMe/hondigagae/issues/75) 때부터 있었다.

## D0. 화면 정의

- **경로**: `/plans/[planId]` — route group `(main)`
- **인증**: **필요**. `proxy.ts` `PROTECTED_PATHS` 에 `'/plans'` 가 이미 있고 매칭이
  `pathname === base || pathname.startsWith(base + '/')` 라 **`/plans/{planId}` 도 이미 걸린다.
  추가 등록이 필요 없다.** 미로그인은 `/login?returnTo=/plans/123` 으로 보내진다.
- **진입 경로**: 목록 행(`plan-row.tsx`) · 홈 `upcoming-plan-row.tsx` · 만들기 성공 후
- **이탈 경로**: 목록(`/plans`) · 장소 상세(`/places/{placeId}`) · 삭제 성공 → `/plans`

> **만들기 성공 후 이동을 이 이슈에서 바꾼다.** 공통명세 S9 가 "상세가 생기면
> `/plans/{planId}` 로 바꾼다" 고 예고해 뒀다. 지금은 `/plans` 로 돌아간다.

## D1. 레이아웃

### 모바일 (390 — 아트보드 01)

단일 칼럼. 위에서 아래로:

```text
제목 · 상태 배지 · 기간 · 총 일수
반려견 카드 (이니셜 · 이름 · 품종 · 특성)  ·  D-day
─────────── 8px 밴드 ───────────
1일차  9/12 (토)          [순서 편집] [이 날 산책]
  일자 판정 (등급 배지 · 최고기온 · reasons)
  ─ 1px ─
  항목 1 · 2 · 3
  [장소 추가]
─────────── 8px 밴드 ───────────
2일차 …
  (비 예보일 때) 비가 오면 갈 만한 실내
─────────── 8px 밴드 ───────────
[일정 확정하기]   ← DRAFT 일 때만
위험 영역 — [일정 삭제]
```

- **8px 밴드는 일자 경계에만 쓴다.** 일자 안(판정 → 항목 → 실내 대안)은 1px 선으로 잇는다.
  밴드가 일자를 나누는 유일한 신호다 (아트보드 01 주석).
- **순번은 썸네일 좌상단 칩이다.** 순번 원 + 썸네일 + 텍스트로 3열을 만들면 390 에서 제목이
  눌린다. 2열(썸네일 · 텍스트)로 유지한다 (아트보드 01 주석).

### 데스크톱 (1024+ — 아트보드 02)

기존 `.rail-layout` 을 그대로 쓴다 — `--rail-context`(400) + 1fr, sticky 좌열, `dvh` 열 구분선.

**`.rail-layout-detail` 변형은 쓰지 않는다.** 그 변형은 장소 상세용이고 **DOM 순서와 데스크톱
배치가 다른 경우**를 위한 것이다. 일정 상세는 모바일에서도 개요·반려견이 맨 위라 **DOM 순서
그대로(aside 먼저)** 두면 두 레이아웃이 같이 성립한다.

| 열              | 내용                                                                      |
| --------------- | ------------------------------------------------------------------------- |
| 좌 400 (sticky) | 제목 · 상태 배지 · 기간 · D-day · 반려견 · **일자별 판정 목차** · 항목 수 |
| 우 가변         | 일자 섹션 (제목 `28/800`) · 확정 버튼 · 위험 영역                         |

- **좌측 "일자별 판정" 세 줄은 앵커 링크다** (`#day1` …). 요약을 읽다가 그 날로 뛴다.
  각 줄 44px, 판정 색을 유지해 **목차이면서 요약**이다 (아트보드 02 주석).
- 폭 상한을 두지 않고 전폭을 쓴다 — 홈·목록·장소 상세와 같은 레일 문법이다.
- 1024 미만에서는 `.rail-layout` 이 grid 가 아니라 DOM 순서대로 쌓인다.

### 긴 한국어 실데이터로 확인할 항목

- 제목 60자(서버 상한) — 좌측 400 에서 `word-break: keep-all` 로 2~3줄
- 반려견 이름 + 품종 + 특성 3개가 한 줄에 들어가는지
- `unavailableReason` 은 서버 문장이라 길이를 가정하지 않는다 (예: "기상 예보는 11일까지만
  제공돼 이 날은 아직 판단할 수 없습니다.")
- 항목 제목 100자(서버 상한)

## D2. 컴포넌트 구성

| 컴포넌트                 | 신규/기존     | 위치                                     | 비고                                           |
| ------------------------ | ------------- | ---------------------------------------- | ---------------------------------------------- |
| `PlanDetailView`         | 신규          | `features/plan/plan-detail-view.tsx`     | client. 조회 3종을 묶는 껍데기                 |
| `PlanDetailSection`      | 신규          | `features/plan/plan-detail-section.tsx`  | 레이아웃 소유 (좌 레일 + 우 본문)              |
| `PlanOverviewPanel`      | 신규          | `features/plan/plan-overview-panel.tsx`  | 좌 레일 — 개요·반려견·판정 목차                |
| `PlanDaySection`         | 신규          | `features/plan/plan-day-section.tsx`     | 한 일자 = 판정 + 항목들 + 실내 대안            |
| `PlanDayVerdict`         | 신규          | `features/plan/plan-day-verdict.tsx`     | 등급 배지 · 기온 · reasons · unavailableReason |
| `PlanItemRow`            | 신규          | `features/plan/plan-item-row.tsx`        | 순번 칩 · 썸네일 · 제목 · 주소 · 거리          |
| `PlanIndoorAlternatives` | 신규          | `features/plan/plan-indoor-alts.tsx`     | A 에서는 **제목만**. 담기는 이슈 C             |
| `PlanStatusAction`       | 신규          | `features/plan/plan-status-action.tsx`   | 확정하기 버튼 + mutation                       |
| `PlanEditModal`          | 신규          | `features/plan/plan-edit-modal.tsx`      | 이름 · 예산                                    |
| `PlanDeleteSection`      | 신규          | `features/plan/plan-delete-section.tsx`  | `pet-delete-section.tsx` 를 본뜬다             |
| `PlanDetailSkeleton`     | 신규          | `features/plan/plan-detail-skeleton.tsx` | `aria-hidden`                                  |
| **`Modal`**              | **기존 공통** | `components/modal.tsx`                   | **#79 에서 추출 완료. 아래 참고**              |

### 재사용할 공통 컴포넌트

`Button` · `Badge` · `Menu`/`MenuAnchor` · `ConfirmModal` · `MetricBadge`/`MetricValue` ·
`ReasonList` · `EmptyState` · `ErrorState` · `Skeleton` · `Surface` · `PetAvatar` ·
`Field`/`Input`/`FormAlert` · `PlanStatusBadge`(기존 `features/plan/plan-status-badge.tsx`)

- **등급 톤은 `MetricBadge`/`MetricValue` 로만 그린다.** 등급 톤 표를 다시 만들지 않는다
  ([#68](https://github.com/8llow8llowMe/hondigagae/issues/68) 에서 일원화했다).
- 판정 근거는 `ReasonList` 를 그대로 쓴다 — 홈·장소 상세와 같은 모양이어야 한다.
- **상태 배지는 `PlanStatusBadge` 재사용이다.** 목록과 같은 규칙(공통명세 S7): 등급 색을
  쓰지 않고, 서버가 모르는 코드를 내려도 초안과 같은 점선으로 그린다.

### 공통 컴포넌트 `Modal` — **이미 있다** (#79 완료)

이름·예산 수정은 **입력이 있는 다이얼로그**라 `ConfirmModal` 로는 안 된다. 다이얼로그 계약
(focus trap · Esc · 스크롤 잠금 · 포커스 복귀 · `role="dialog" aria-modal="true"`)은
[#79](https://github.com/8llow8llowMe/hondigagae/issues/79) 에서 `components/modal.tsx` 로
추출했고 `ConfirmModal` 도 그것을 쓴다. **`PlanEditModal` 은 새로 만들지 말고 이 `Modal` 을 쓴다.**

```tsx
<Modal
  open={open}
  onClose={close}
  title="일정 수정"
  size="md" // 입력 폼은 md. 확인 다이얼로그가 sm 이다
  footer={<>…취소 · 저장…</>}
>
  …필드…
</Modal>
```

- `role` 기본값이 `dialog` 다. **`alertdialog` 는 되돌릴 수 없는 확인에만** 쓴다 (그쪽은
  `ConfirmModal` 이 이미 처리한다).
- 제목·설명 id 는 `Modal` 이 `useId()` 로 만들어 `aria-labelledby`/`aria-describedby` 에 묶는다.
  **호출부가 id 를 만들지 않는다.**
- 계약 테스트는 `src/components/modal.test.ts` 다.

**`ConfirmModal` 의 공개 API 는 바꾸지 않는다** — 호출부(`pet-delete-section.tsx`)를 건드리지
않아야 이 리팩터가 A 이슈 diff 를 키우지 않는다.

## D3. 데이터 흐름

```text
서버                       클라이언트
────────────────────────   ──────────────────────────────────────────
readSession()
GET /plans/{planId}   ────▶ (프리페치 완료 상태로 hydrate)
  fetchQuery + try/catch    ├─ GET /plans/{planId}/weather   (판정)
  404 → notFound()          ├─ GET /members/me/pets          (반려견)
                            └─ GET /places/{placeId} × N     (항목 보강)
today = new Date()
```

| 항목                   | 내용                                                                                                                                           |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| API                    | `GET /plans/{planId}` (서버) · `/weather` · `/members/me/pets` · `/places/{placeId}` × N (클라이언트)                                          |
| React Query key        | `planKeys.detail(planId)` (기존) · `planKeys.weather(planId)` (신규) · `petKeys.list()` (기존) · **`placeKeys.detail(placeId)` (기존 재사용)** |
| staleTime / retry      | 일정·판정 = `PLAN_QUERY_OPTIONS`(30s / 10m). 서버 `fetchQuery` 는 **`retry: false`**                                                           |
| mutation 후 invalidate | `PUT`/`DELETE` 응답이 `PlanDetailResponse` 라 **`setQueryData` 로 갈아끼우고** `planKeys.list()` 만 invalidate                                 |
| 폴링 여부              | 없음                                                                                                                                           |

### 서버 — `fetchQuery` + try/catch, `loading.tsx` 없음

장소 상세(`app/(main)/places/[placeId]/page.tsx`)와 **같은 골격**이다.

- `prefetchQuery` 는 에러를 삼켜서 404(그런 일정이 없음)와 5xx(일시 장애)를 못 가른다.
  `fetchQuery` 는 던지므로 잡아서 판정한다.
- `retry: false` — 전역 기본값(5xx 2회)을 상속하면 백엔드가 죽었을 때 서버 렌더가 재시도
  백오프만큼 통째로 블로킹된다 (`architecture-guide.md` §9).
- **이 세그먼트에 `loading.tsx` 를 두지 않는다.** 경계가 있으면 응답이 먼저 스트리밍되기
  시작해 뒤따르는 `notFound()` 가 HTTP 상태를 못 바꾼다 (soft 404, `architecture-guide.md` §7).
  `app/(main)/plans/` 에도 `loading.tsx` 가 없어 그룹 분리는 필요 없다 — **생기면 그때
  `(list)` 그룹으로 옮긴다.**
- `not-found.tsx` · `error.tsx` 를 세그먼트에 둔다.
- **`today` 를 서버에서 만들어 내려보낸다.** 클라이언트가 따로 `new Date()` 를 부르면 자정
  근처에서 서버 렌더와 하이드레이션의 D-day 가 하루 갈린다 (목록과 같은 결정).

`generateMetadata` 는 두지 않는다 — 보호 화면이라 크롤러가 못 들어오고, 제목을 위해 백엔드를
한 번 더 부를 이유가 없다. `layout` 의 기본 제목을 쓴다.

### 항목 보강 — 항목당 `GET /places/{placeId}`

> **[#86](https://github.com/8llow8llowMe/hondigagae/issues/86) 이 반영됐다.** 백엔드가
> `PlanItemDetail.place` 로 `addr1` · `indoor` · `firstImage` · `lat` · `lng` 를 함께 내려준다
> (요청 한 번, 중복 제거). **아래의 항목당 보강은 이제 필요 없다** — 걷어내는 것은 FE 후속
> 작업이고, 그때까지는 아래 서술이 현재 구현이다.

`PlanItemDetail` 은 `planItemId` · `day` · `sequence` · `itemType` · `targetId` · `title` ·
`memo` · `startTime` 뿐이었다. **주소 · 실내 여부 · 이미지 · 좌표가 없고, 배치 조회 API 도 없었다.**

> **실내 여부는 #80 에서 못 붙였다가 [#112](https://github.com/8llow8llowMe/hondigagae/issues/112)
> 에서 붙였다.** #16 으로 상세 응답에 `indoor` 가 들어와 보강 결과에서 그대로 온다 —
> 목록 캐시를 꺼내 쓰는 우회는 끝까지 쓰지 않았다. **행 메타 줄은 `주소 · 실내` 다**
> (조립은 `lib/place/meta.ts`). `indoor` 가 `null` 이면 낱말이 빠진다 — 여기에는 실내 필터가
> 없어 "미확인" 배지를 둘 자리가 없고, `false`(야외)로 단정하지도 않는다.

- `useQueries` 로 병렬 조회한다. **key 는 `placeKeys.detail(placeId)` 재사용** — 장소 상세
  화면과 캐시를 공유해서, 상세를 보고 온 항목은 요청이 아예 나가지 않고 여기서 본 장소는
  장소 상세로 이동할 때 즉시 뜬다.
- **조회 대상은 `itemType ∈ {PLACE, MEAL, LODGING}` 이고 `targetId !== null` 인 항목뿐이다.**
  `WALK` 의 `targetId` 는 `walk_course.id` 라 장소가 아니고(백엔드 `PLACE_TARGET_TYPES` 주석),
  `MOVE` 는 대상이 없다. 이 둘을 `/places/{id}` 로 부르면 남의 id 로 404 를 만든다.
- **실패한 항목은 제목만 남기고 행은 살린다.** 일정 자료는 우리 DB, 장소는 다른 서비스다
  (공통명세 S8 과 같은 태도).
- 한 일정의 항목 수는 아트보드 기준 6~8개다. `staleTime` 이 장소 기본값이라 재방문에서는
  거의 나가지 않는다.

> **BE 후속 요청**: `PlanItemDetail` 에 place 요약(주소 · `indoor` · `firstImage` · `lat`/`lng`)을
> 넣으면 이 N번 왕복이 사라진다. 이슈로 뗀다.

### 거리 — FE 하버사인, **직선거리임을 문구로 밝힌다**

서버에 거리 필드가 없다. 위 보강으로 좌표가 따라오므로 화면이 계산한다.

- `src/lib/geo/distance.ts` 에 하버사인을 추가하고 **기존 `formatDistance()`(미터 입력)를
  그대로 쓴다** — 긴급 시설과 같은 표기여야 한다.
- 좌표가 유효하지 않은 장소가 있다 → 기존 `toLatLng()` 가 null 을 돌려준다. **한쪽이라도
  null 이면 그 행의 거리 문구를 렌더하지 않는다.** 0km 로 쓰지 않는다.
- **`직선 4.1km` 처럼 "직선" 을 붙인다.** 제주 산간·해안도로에서 직선거리와 주행거리는 크게
  다르다. 아트보드의 `4.1km` 를 그대로 쓰면 주행거리로 읽힌다.
- 기준점:
  - **숙소 기준** — **앞선 날**의 마지막 `LODGING` 항목. 있으면 그날 첫 항목에
    `숙소에서 직선 N km`
    > **구현하며 "그날" 을 뺐다.** 그날의 숙소를 기준으로 쓰면 아직 가지 않은 곳에서
    > 출발한 것이 된다 — 실렌더에서 1일차 첫 항목에 `숙소에서 직선 14.8km` 가 붙었고
    > 그 숙소는 그날 **저녁에** 체크인할 곳이었다. "그날" 을 넣어도 얻는 것이 없다:
    > 숙소가 그날 첫 항목이면 자기와의 거리(0km)라 잡음이고, 둘째 항목부터는
    > **직전 항목 규칙이 이미 그 숙소를 기준으로 잡는다.**
  - **순번 간** — 2번째 항목부터 직전 항목까지 `직선 N km 이동`
  - 숙소가 없으면 첫 항목에는 아무것도 붙이지 않는다
- **긴 이동 경고 임계값은 30km.** 넘으면 그 행만 경고 tint + `직선 41.2km 이동 — 하루 이동이
깁니다.` 별도 경고 배지를 만들지 않는다. **이게 편집 동기를 만든다** (아트보드 01 주석).

### 판정 — `GET /plans/{planId}/weather`

`PlanWeatherResponse.days` 는 **일정 일수만큼 항상 채워진다.** 배열 길이로 성공/실패를
판단하지 않는다. `day` 로 일자 섹션에 합류시킨다.

- `score` · `suitabilityLevel` 이 null 이면 **판정을 못 낸 것이지 낮은 것이 아니다.**
  `unavailableReason` 을 **문장 그대로** 쓰고, 0점이나 회색 등급 배지로 만들지 않는다
  (아트보드 01 주석).
- `reasons` → `ReasonList`.
- `petConditionApplied === false` 면 반려견 특성 조회에 실패해 일반 조건으로 판정한 것이다 →
  판정 옆에 그 사실을 한 줄로 알린다.
- **판정 조회 실패(5xx)는 섹션 단위 오류다.** 전체 화면을 덮지 않는다 — 일정 자료는 우리 DB,
  판정은 외부 예보다 (아트보드 06 ③). 일자 섹션마다 "이 날 판정을 불러오지 못했어요 · 다시
  시도" 를 넣고 항목은 그대로 보여준다.

### `이 날 산책` 버튼

`GET /places/{placeId}/walk-safety` 로 간다. 대상은 `PlanDayWeatherItem.representativePlaceId`
(그날 판정 기준이 된 장소)다. **null 이면 버튼을 렌더하지 않는다** — 기준 장소가 없으면
부를 대상이 없다.

## D3-1. `types/plan.ts` 드리프트 — 이 이슈에서 고친다

`PlanDailyWeatherItem` 이 실제 DTO 와 어긋나 있다. 방금 받은 백엔드
`fc7d8af`(기상청 중기예보를 이어 붙여 예보 범위를 11일로 확장) 에서 바뀐 것으로 보인다.

| 현재 `types/plan.ts`                  | 실제 `PlanDailyWeatherItem`                                |
| ------------------------------------- | ---------------------------------------------------------- |
| `skyState: CodeNameMetadata \| null`  | **`skyStateName: string`** — metadata 객체가 아니라 문자열 |
| `precipitationType: CodeNameMetadata` | **`precipitationTypeName: string`**                        |
| —                                     | **`forecastSourceCode`** · **`forecastSourceName`** 누락   |
| —                                     | **`maxWindSpeed`** · **`maxHumidity`** 누락                |

- **`forecastSourceCode === 'MID_TERM'` 이면 "대략적인 값"이다** (스키마 설명 명시). 그 일자
  판정 옆에 출처(`forecastSourceName`)를 한 줄로 붙여 정밀도 차이를 감추지 않는다.
- 이 드리프트는 `_index.md` 의 "명세와 실제 계약이 갈린 곳" 표에도 추가한다.

## D4. 상호작용

| 트리거                    | 동작                                            | 결과                                                                   |
| ------------------------- | ----------------------------------------------- | ---------------------------------------------------------------------- |
| 항목 행 클릭              | `/places/{targetId}` 로 이동                    | 장소 상세. `targetId` 가 없으면(=`MOVE`) 링크를 만들지 않는다          |
| 좌측 판정 목차 줄 클릭    | `#day{n}` 앵커                                  | 해당 일자로 스크롤. 데스크톱만                                         |
| `이 날 산책`              | `/places/{representativePlaceId}/…` 산책 위험도 | `representativePlaceId` 가 null 이면 버튼 없음                         |
| `일정 확정하기`           | `PUT /plans/{planId}` `{ status: 'CONFIRMED' }` | 성공 → `setQueryData` + 목록 invalidate. 버튼이 사라지고 배지만 남는다 |
| 더보기 → `이름·예산 수정` | `PlanEditModal` 열기 → `PUT /plans/{planId}`    | `{ title, budget }` 만 보낸다                                          |
| 더보기 → `일정 삭제`      | `ConfirmModal` → `DELETE /plans/{planId}`       | 성공 → `/plans` 로 replace + 목록 invalidate                           |
| 판정 섹션 `다시 시도`     | `planKeys.weather(planId)` refetch              | 그 섹션만 다시 그린다                                                  |

### `PUT` 은 부분 수정이다 — 보내지 않은 필드는 유지된다

`PlanUpdateRequest` 는 `title` · `startDate` · `endDate` · `budget` · `status` 를 받고
**null 인 필드는 기존 값을 유지**한다 (`PlanCommandProcessor.updatePlan`).

- 확정하기는 `{ status: 'CONFIRMED' }` 만 보낸다
- 이름·예산 수정은 `{ title, budget }` 만 보낸다
- **예산을 비우는 방법이 없다.** `budget: null` 은 "유지" 로 읽힌다. 폼에서 예산을 지우면
  `0` 으로 보낸다 — `@PositiveOrZero` 라 0 은 유효하다. 문구로 그렇게 안내한다.

### 기간 수정은 이 이슈에서 하지 않는다

`PlanCommandProcessor.updatePlan` 은 **기간을 줄여도 항목을 정리하지 않는다.** 3일 일정을
2일로 줄이면 `day: 3` 항목이 그대로 남고, 상세 응답에 `totalDays` 를 넘는 항목이 섞여 온다.

→ **프론트가 고아 항목을 만드는 경로를 열지 않는다.** BE 이슈로 뗀다.

다만 **다른 경로로 이미 생긴 고아 항목은 이 화면이 만날 수 있다.** `totalDays` 만큼 일자
섹션을 그리고, 범위를 벗어난 항목은 맨 아래 **"여행 기간 밖 항목"** 으로 모아 보여준다.
**숨기지 않는다** — 숨기면 사용자가 자료가 사라진 것을 모른다.

## D5. 상태별 화면

| 상태                          | 표시                                                                                                                              | 문구 (확정)                                                             |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| loading                       | **섹션 단위**. 일정 본문은 서버 프리페치라 첫 페인트에 이미 있다 — 스켈레톤은 판정·반려견·항목 보강 자리에만 쓴다 (`aria-hidden`) | —                                                                       |
| success                       | 위 레이아웃                                                                                                                       |                                                                         |
| 일정 없음 (404)               | `not-found.tsx`                                                                                                                   | `찾을 수 없는 일정이에요` / `삭제됐거나 주소가 잘못됐어요.` + 목록 링크 |
| 일정 조회 실패 (5xx)          | `ErrorState` + 재시도                                                                                                             | `일정을 불러오지 못했어요` / `잠시 후 다시 시도해 주세요.`              |
| **판정만 실패 (5xx)**         | **일자 섹션 안에만** 인라인 + 다시 시도                                                                                           | `이 날 판정을 불러오지 못했어요.`                                       |
| 판정 근거 없음 (`score` null) | 등급 배지 대신 중립 문장                                                                                                          | 서버 `unavailableReason` **그대로**                                     |
| 반려견 조회 실패              | **숨김** — 반려견 카드만 빠진다                                                                                                   | —                                                                       |
| 항목 장소 조회 실패           | **숨김** — 제목만 남고 행은 유지                                                                                                  | —                                                                       |
| 항목이 0개                    | 일자 섹션 안 빈 안내                                                                                                              | `이 날은 아직 담은 곳이 없어요.` (+ 이슈 C 에서 `장소 추가`)            |
| 기간 밖 항목 있음             | 맨 아래 별도 섹션                                                                                                                 | `여행 기간 밖 항목` / `기간이 줄어들면서 남은 항목이에요.`              |

- **404 판정은 "없는 일정"과 "남의 일정"을 구분하지 않는다.** 백엔드가 본인 소유가 아니면
  404 로 응답한다(컨트롤러 설명 명시). 화면도 존재 여부를 흘리지 않는다.
- `PLAN_114`(숫자가 아닌 `planId`) 는 **400 이지 404 가 아니다** — `@PathVariable long` 이다.
  장소 상세의 `PLACE_113` 과 같은 상황이고, **같은 주소를 다시 불러도 같은 400 이라 재시도를
  주지 않는다.** 중립 톤 `EmptyState` + 목록 링크를 쓴다.

## D6. 접근성

- 더보기(icon-only) `aria-label="일정 관리"`. `Menu` 가 키보드 순회를 이미 보장한다.
- 좌측 판정 목차는 **링크 목록**(`<a href="#day1">`)이다. 각 줄 44px.
- 일자 섹션은 `<section aria-labelledby="day1">`, 제목이 `<h2 id="day1">`.
- **판정 등급을 색으로만 전달하지 않는다.** `MetricBadge` 가 등급 문구(`suitabilityLevel.name`)를
  함께 낸다. 긴 이동 경고 tint 도 문장이 함께 간다.
- 항목 행: 링크 안에 링크를 넣지 않는다 (`pet-row.tsx` 와 같은 규칙). 행 전체가 하나의 링크다.
- 삭제 `ConfirmModal` — 취소가 좌측이고 기본 포커스가 취소다 (`ConfirmModal` 이 보장).
- `prefers-reduced-motion` — 앵커 스크롤에 `scroll-behavior: smooth` 를 쓰면 그 미디어쿼리에서
  꺼야 한다 ([#15](https://github.com/8llow8llowMe/hondigagae/issues/15) 에서 정한 규칙).

## D7. 테스트 케이스

`renderToStaticMarkup` 문자열 assertion (이 저장소 방식, `testing-guide.md`).

**순수 로직**

- `haversineMeters()` — 알려진 두 좌표(제주공항 ↔ 성산일출봉)로 오차 범위 확인
- 좌표 한쪽이 null → 거리 없음 (0 이 아니라 null)
- 숙소 기준 선택 — 그날 `LODGING` / 앞선 날 `LODGING` / 없음
- 일자 그룹핑 — `totalDays` 만큼 섹션이 생기고, `day > totalDays` 항목이 "기간 밖" 으로 분리
- 보강 대상 필터 — `WALK`·`MOVE` 는 `/places` 를 부르지 않는다
- 30km 임계값 경계 (29.9 / 30.0 / 30.1)

**렌더 분기 (404 / 5xx 필수)**

- 404 → `notFound()` 경로 (장소 상세 테스트와 같은 방식)
- 일정 5xx → `ErrorState`
- **판정 5xx → 일자 섹션 안에만 오류가 나오고 항목은 남는다** (아트보드 06 ③)
- `score === null` → 등급 배지 대신 `unavailableReason` 문장
- `representativePlaceId === null` → `이 날 산책` 버튼 없음
- 반려견 조회 실패 → 카드만 빠지고 나머지는 그대로
- `status.code` 가 미지의 코드 → 초안과 같은 점선 + `name` 그대로 (공통명세 S7)
- `forecastSourceCode === 'MID_TERM'` → 출처 문구가 나온다

**접근성 계약**

- 일자 섹션 `aria-labelledby` 가 `<h2 id>` 를 가리킨다
- 더보기 버튼 `aria-label`
- 등급이 색 외에 문구로도 전달된다

**mock**

`src/lib/api/mock/plan-data.ts` 에 상세·수정·삭제·`/weather` 를 추가한다. **백엔드보다
느슨하거나 엄격해서는 안 된다** (`pet-data.ts` 와 같은 규칙). 특히:

- 숫자가 아닌 `planId` → **400 `PLAN_114`**, 없는 숫자 `planId` → **404 `PLAN_001`**
- `PUT` 은 부분 수정 — 보내지 않은 필드 유지
- `/weather` 의 `days` 는 **항상 `totalDays` 길이**, 그중 한 일자는 `score: null` +
  `unavailableReason`, 한 일자는 `indoorAlternatives` 있음

## D8. 미결 사항

| #   | 항목                                                                                          | 선택지                                                                   | 추천                                                                                                    |
| --- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| 1   | 아트보드의 `33.4℃` 가 계약에 없다 (reasons 의 "최고기온 31도" 와 다른 값 — 체감온도로 보인다) | ① `maxTemperature` 로 대체 ② 큰 숫자 자리를 없앤다 ③ BE 에 체감온도 요청 | **① + BE 이슈.** 지어내지 않는다                                                                        |
| 2   | 항목당 `GET /places/{id}` N번                                                                 | ① 그대로 ② BE 에 place 요약 포함 요청                                    | **① 로 가고 ② 를 이슈로 뗀다.** 화면을 막지 않는다                                                      |
| 3   | 기간을 줄여도 항목이 남는다 (`updatePlan` 이 정리하지 않음)                                   | ① 기간 수정을 안 연다 ② FE 가 미리 경고 후 진행                          | **①.** 프론트가 고아 항목을 만드는 경로를 열지 않는다                                                   |
| 4   | 긴 이동 임계값 30km                                                                           | ① 30km ② 50km ③ 그날 총 이동 기준                                        | **①.** 아트보드가 41.2km 를 "길다" 로 본다                                                              |
| 5   | 예산을 비우는 방법이 없다 (`budget: null` = 유지)                                             | ① `0` 으로 보낸다 ② BE 에 clear 수단 요청                                | **① + 문구 안내.** `@PositiveOrZero` 라 0 은 유효하다                                                   |
| 6   | 데스크톱 좌패널의 `경로 주변 병원 · 약국`                                                     | ① 이 이슈에서 뺀다 ② `/emergencies/facilities` 로 붙인다                 | **①.** 일정 좌표 기준점 정의가 따로 필요하다 — 별도 이슈                                                |
| 7   | 아트보드의 `혼잡도 정보 없음`                                                                 | —                                                                        | **범위 밖.** [#29](https://github.com/8llow8llowMe/hondigagae/issues/29) BE 미착수                      |
| 8   | 아트보드의 `후기 쓰기 · 후기 보기`                                                            | —                                                                        | **범위 밖.** `review` 패키지 없음 (공통명세 S2 와 같은 판단)                                            |
| 9   | `startTime` 은 계약에 있으나 표시하지 않는다                                                  | ① 표시 안 함 ② 표시                                                      | **①.** 아트보드 헤더 주석이 "시간 없음" 으로 못박았다                                                   |
| 11  | `memo` 도 표시하지 않는다 (D2 가 행 구성에 넣지 않았다)                                       | ① 표시 안 함 ② 행에 한 줄 추가                                           | **①로 구현했으나 재검토 필요.** 사용자가 적은 값이 화면에서 사라진다 — 이슈 B 에서 편집과 함께 판단한다 |
| 10  | Swagger 실호출 대조를 못 했다                                                                 | —                                                                        | [#67](https://github.com/8llow8llowMe/hondigagae/issues/67) 에서 몰아서 대조한다                        |
