/** 여러 화면이 공유하는 문구. 화면 고유 문구는 각 도메인 파일에 둔다 */
export const commonMessages = {
  retry: '다시 시도',
  loading: '불러오는 중',
  /** 오버레이·선택 카드를 닫는 아이콘 버튼의 이름 */
  close: '닫기',

  /** 5xx·무응답 */
  temporaryErrorTitle: '잠시 문제가 생겼어요',
  temporaryErrorDescription: '잠시 후 다시 시도해 주세요.',

  /** 400 */
  validationErrorTitle: '요청 조건이 올바르지 않아요',

  /** 401 */
  unauthorizedTitle: '로그인이 필요해요',
  unauthorizedDescription: '로그인 후 다시 이용해 주세요.',

  /** 403 등 */
  forbiddenTitle: '이용할 수 없는 요청이에요',

  /*
    전역 404 — **주소가 어느 라우트와도 안 맞을 때다** (#494). 화면 안에서 잡히는 404
    (없는 장소·일정·반려견)는 각 도메인 파일이 자기 문구를 갖는다. 여기가 말할 수 있는
    것은 "그런 주소가 없다" 뿐이라 무엇이 없는지 짐작해 말하지 않는다.
  */
  notFoundTitle: '없는 주소예요',
  notFoundDescription: '주소가 잘못됐거나 사라진 화면이에요.',
  /** 돌아갈 곳 둘 — 홈이 먼저다. 어디서 헤맸든 처음으로 돌아가는 길이 기본이다 */
  notFoundHomeAction: '홈으로',
  notFoundPlacesAction: '장소 찾기',

  /*
    오프라인 (#912). **서버 장애와 다른 말을 한다** — 원인이 서버가 아니라 내 연결이라는 것을
    알아야 재시도 대신 이동(전파가 잡히는 곳으로)을 택한다. 여행 중 산간·해안에서 잦다.
    "자동으로 이어져요" 는 사실이다: React Query 가 끊긴 동안 조회를 멈췄다가
    (`networkMode: 'online'`) 연결되면 다시 부른다(`refetchOnReconnect`).
  */
  offlineBanner: '인터넷 연결이 끊겼어요. 다시 연결되면 자동으로 이어져요.',
  offlineTitle: '인터넷에 연결되어 있지 않아요',
  offlineDescription: '연결되면 다시 시도할 수 있어요. 전파가 잡히는 곳으로 옮겨 보세요.',

  /**
   * **사용자가 한 일을 서비스가 대신 서술하지 않는다.** "확인했어요" 는 사용자의 행동이고,
   * 목록 끝에 붙는 이 줄이 말할 수 있는 것은 목록의 상태뿐이다 (디자인 리뷰 #15).
   */
  listEnd: '마지막 장소예요',

  // ── 체감온도 근거의 각주 (#317) ───────────────────────────────────────────
  /**
   * `humidity` 를 **단독 수치로 세우지 않는다.** `습도 60%` 만 있으면 사용자가 할 수 있는
   * 일이 없다 — 바로 위 `feelsLikeBasis` 가 *"판정 시각의 기온과 상대습도로 습구온도를
   * 구해 산출하며"* 라고 말하고, 이 값이 그 문장이 말하는 입력의 실제 수치다. 둘을 붙여
   * 두면 서로를 설명한다.
   *
   * `{value}` 치환.
   */
  feelsLikeHumidity: '계산에 쓴 상대습도 {value}%',
  /**
   * `weatherProviderName`. **출처를 밝히는 것은 이 서비스의 정체성과 맞는다** —
   * `DESIGN.md` §1 이 시각 컨셉을 "데이터 신뢰형" 으로 잡았고, 판정의 근거를 감추지 않는
   * 것이 이 저장소가 반복해 세운 규칙이다.
   *
   * **`null` 이면 이 조각을 통째로 뺀다.** 근거 문장은 출처 없이도 성립한다.
   *
   * `{name}` 치환.
   */
  feelsLikeProvider: '출처 {name}',
  /** 필터 레일을 건너뛰고 목록으로 (#472) */
  skipToList: '목록으로 건너뛰기',

  // ── 등급 배지의 축 라벨 (#652) ────────────────────────────────────────────
  /*
    **같은 낱말이 두 축에 쓰인다.** 서버 적합도 `MEDIUM` 도 `보통` 이고 혼잡도 `MODERATE`
    도 `보통` 이라, 장소 상세 이름 옆 배지가 어느 축인지 화면에 단서가 없었다 (진단 D-2).
    어휘를 다시 짓는 대신 **배지 앞에 축 이름만** 붙인다 — 서버 `name` 을 그대로 렌더하는
    규칙(api-integration-guide.md §6)을 건드리지 않는 쪽이다.

    **두 글자 형(`적합` · `혼잡`)을 쓰지 않는다.** 실제 등급어에 붙이면 무너진다 —
    `혼잡 한산` 은 스스로 모순이고 `적합 여행 적합` 은 같은 말을 두 번 한다. `-도` 형은
    축 이름이라 어떤 등급어 앞에도 설 수 있다.

    붙이는 축은 이 둘뿐이다. 산책 안전(`안전·주의·위험`)·기상특보(`폭염 경보`)·속성
    배지(`실내 여부 미확인`)는 충돌 상대가 없거나 문구가 이미 축을 말한다
    (등급배지-축라벨-세부명세 D5).
  */
  /** `DESIGN.md` §2-3 등급 표의 "대표 문구" 열이 이미 `적합도 높음/보통/낮음` 이다 */
  metricAxisSuitability: '적합도',
  /** `home.congestionUnknown` 이 예외적으로 이미 쓰던 접두어다 — 규칙으로 올렸다 */
  metricAxisCongestion: '혼잡도',
  /**
   * 일정 항목 행 전용 (#625 · 일정상세-세부명세 D15-10 미결 1). **`-도` 형이 아니다** —
   * `산책도 주의` 는 말이 되지 않는다. `산책 안전`·`산책 주의`·`산책 위험` 세 값 모두
   * 문장이 선다.
   */
  metricAxisWalkSafety: '산책',
} as const
