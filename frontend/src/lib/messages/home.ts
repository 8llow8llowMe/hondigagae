/**
 * 홈 화면 문구 — 공통명세 S4-4 가 정본이다.
 *
 * 어미는 **해요체로 통일한다.** 서버가 내려주는 근거 문장(`description`)은 합니다체라
 * 섞이지만, **출처를 바꾸지 않는다** — 서버 문구를 FE 가 다시 쓰지 않는다
 * (docs/styling-guide.md §7).
 */
export const homeMessages = {
  walkTodayLabel: '오늘 산책',
  /**
   * 페이지 최상단 특보 줄의 접근성 이름 (#349).
   *
   * **화면에 글자로 그리지 않는다.** 그 줄이 말하는 것은 전부 서버 값(배지 `type.name` +
   * `level.name`, 옆 문장 `type.description`)이고, 여기에 우리 제목을 하나 더 얹으면
   * 같은 사실이 다시 두 번이 된다 — 이 이슈가 없앤 바로 그 모양이다.
   */
  warningStripLabel: '기상특보',
  /**
   * 판정 요약줄의 체감온도 ([#259](https://github.com/8llow8llowMe/hondigagae/issues/259)).
   * **장소 상세와 같은 `feelsLikeCelsius`(시각 기준)라 같은 이름으로 부른다** — 예전에는
   * 여기만 `체감 열지수` 였다. 같은 물리량을 화면마다 다르게 부르면 사용자는 서로 다른
   * 값으로 읽는다. 하루 최대 쪽은 `최고 체감온도` 로 갈린다.
   *
   * **근거 필드가 `heatIndexCelsius` → `feelsLikeCelsius` 로 바뀌었다** (#292). 라벨 문구는
   * 그대로 맞다 — 판정 기준이 NOAA 열지수에서 기상청 체감온도로 교체됐고, 화면은 처음부터
   * 그 자리를 `체감온도` 라고 불러 왔다. **바뀐 것은 이름이 아니라 그 이름이 가리키던 값**이다.
   */
  feelsLikeLabel: '체감온도',
  /**
   * 체감온도 옆 물음표의 이름 (#313). 아이콘만 든 버튼이라 이 낱말이 유일한 이름이고,
   * 모바일에서는 `BottomSheet` 의 제목이 된다.
   *
   * **`도움말` 이라고 쓰지 않는다.** 한 화면에 이런 물음표가 여럿 서게 되므로
   * (열지수 · 노면 추정치 · 권역 점수), 이름이 대상을 말해야 무엇의 설명인지 구분된다.
   */
  feelsLikeBasisLabel: '체감온도 근거',
  /**
   * 노면온도의 라벨 ([#269](https://github.com/8llow8llowMe/hondigagae/issues/269)).
   *
   * **`(아스팔트)` 를 넣는다.** "노면" 만으로는 흙길·잔디도 떠올리게 되는데 이 추정식은
   * 아스팔트 기준이다. 기온 29℃ 인 날 `56.0℃` 만 보고 "온도가 잘못됐다" 는 제보가 실제로
   * 있었다 — 값이 아니라 **그 값이 무엇인지**가 전달되지 않은 것이다.
   *
   * 곡선 셀에서는 **보조기기 전용**으로 쓴다. 3rem 폭에 라벨을 적을 자리가 없어 눈으로는
   * 위치(막대 위=기온, 아래=노면)와 범례가 가르고, 스크린리더에는 이 낱말이 간다.
   */
  pavementLabel: '추정 노면(아스팔트) 온도',
  /**
   * 기온의 라벨 (#269). **곡선 셀의 숫자가 하나뿐이라 노면온도를 기온으로 읽었다** —
   * API 가 `temperature` 를 함께 주는데 화면이 버리고 있었다. 둘을 나란히 두면
   * "기온은 괜찮은데 지면이 뜨겁다" 는 이 서비스의 요점이 그대로 전달된다.
   */
  temperatureLabel: '기온',
  /*
    `saferWindowLabel` 을 걷었다 (#349). 홈 판정에서 `saferWindow` 줄을 뺐고 — "오늘 언제
    나가나" 는 골든타임 섹션 하나가 답한다 — 이 문구를 쓰는 자리가 사라졌다. 장소 상세는
    자기 문구(`messages.place.detailSaferWindow`)를 쓴다.
  */
  basisSuffix: '기준',
  /**
   * 기준이 사용자가 고른 장소가 아니라 대표 지점일 때 기준 줄 아래 한 줄
   * (홈-첫방문-판정-세부명세 D4).
   *
   * **장소 이름을 여기 적지 않는다.** 이름은 서버 `placeTitle` 이 바로 위 줄에서 이미
   * 말했고, FE 가 한 번 더 적으면 서버 데이터가 갈릴 때 화면이 없는 장소를 부른다.
   *
   * 뒷문장이 **약속**이다 — 장소 상세를 열면 그 id 가 저장되고(`writeRecentPlaceId`)
   * 홈에 돌아왔을 때 기준이 바뀌면서 이 줄이 사라진다. 화면이 실제로 그렇게 움직인다.
   */
  basisDefaultNote: '제주시 대표 지점이에요. 장소를 보면 그곳 기준으로 바뀌어요',
  reasonsLink: '판정 근거 보기',

  // ── 오늘의 산책 골든타임 (#158) ────────────────────────────────────────────
  goldenHeading: '오늘 산책하기 좋은 시간',
  /**
   * 무엇을 기준으로 잰 곡선인지 밝힌다 (#180).
   *
   * **위치를 못 얻었을 때 그 사실을 감추지 않는다.** 다만 거부·타임아웃·미지원을 구분해
   * 적지는 않는다 — 이 섹션은 거리를 표시하지 않아 실패 종류가 사용자의 다음 행동을
   * 바꾸지 않는다 (`/emergency` 는 거리를 쓰므로 거기서는 구분한다).
   */
  goldenBasisCurrent: '현재 위치 기준',
  goldenBasis: '제주시 기준',
  /**
   * 추천 구간이 없는 날. **"그나마 이때가 낫다" 를 쓰지 않는다** — 서버가 일부러 구간을
   * 주지 않는 날이고, 대안을 지어내면 사용자가 그것을 허락으로 읽는다 (`GoldenWalkWindow`).
   */
  goldenNone: '오늘은 나가지 않는 편이 좋아요',
  /**
   * **`ALL_HOURS_RISKY` 일 때만 쓴다** ([#270](https://github.com/8llow8llowMe/hondigagae/issues/270)).
   * 예전에는 `goldenStart` 가 null 이기만 하면 이 문장이 나갔는데, 경보로 **보류**된 날에도
   * 같은 말을 해서 초록 곡선 위에 "모두 위험 등급" 이 얹혔다. 이 문장이 참인 상태는 하나뿐이다.
   */
  goldenNoneDesc: '남은 시간이 모두 위험 등급이에요. 실내에서 보내는 편이 안전해요.',
  /**
   * 경보로 추천을 보류한 날 (#270). **판정이 아니라 보류다** — 그래서 위험 톤을 쓰지 않고
   * 곡선도 그대로 남긴다 (`NoForecast` 와 같은 판단).
   *
   * **곡선이 초록인데 왜 추천이 없는지**를 설명하는 것이 이 문구의 일이다. 그 어긋남이
   * 이 이슈의 제보였다 — 이유를 말하지 않으면 사용자는 화면이 고장 났다고 읽는다.
   */
  goldenSuppressed: '경보가 발효 중이라 추천하지 않아요',
  goldenSuppressedDesc:
    '기상특보 경보가 있는 날은 시간대가 좋아 보여도 추천하지 않아요. 곡선은 근거로 그대로 두었어요.',
  goldenPavementNote: '노면(아스팔트) 온도는 추정치예요',
  /**
   * 곡선의 행 라벨 (#269 → 후속). **범례를 대신한다.**
   *
   * 예전에는 곡선 아래에 `위는 기온 · 아래는 노면(아스팔트)` 한 줄을 두고 위치로 갈랐다.
   * 두 가지가 어긋났다.
   *
   *  - **읽고 나서 위로 되돌아가 대응시켜야 한다.** 셀 안에서 두 숫자를 가르는 채널이
   *    색 하나뿐이라(`text-fg` 대 `text-fg-muted`), 범례를 안 읽으면 어느 쪽이 어느
   *    쪽인지 알 수 없었다 — DESIGN.md §2-3 이 금지한 "색이 유일한 채널" 이다.
   *  - **`노면(아스팔트)` 가 두 가지로 읽힌다.** 괄호는 같은 것을 달리 부른 것인데
   *    나란히 놓이니 항목 둘로 보인다. 이 화면을 만든 사람이 스스로 위아래를 반대로
   *    기억한 것이 이 변경의 발단이었다.
   *
   * 그래서 낱말을 곡선 **왼쪽 고정 열**로 옮긴다. 되돌아가 대응시킬 것이 없어지고,
   * 괄호는 아래 `goldenPavementNote` 가 이미 말하므로 라벨에서 뗀다.
   */
  goldenCurveRowTemperature: '기온',
  goldenCurveRowPavement: '노면',
  /**
   * 곡선을 좌우로 넘기는 화살표의 이름.
   *
   * **아이콘만 든 버튼이라 이름이 여기서만 나온다** — `aria-label`(보조기기)과
   * `title`(마우스 호버) 양쪽에 같은 낱말을 쓴다 (`Chip` 의 `label` 과 같은 규칙).
   *
   * "이전 / 다음" 만으로는 무엇의 이전인지 알 수 없다. 같은 화면에 시간대 곡선과 권역
   * 목록이 함께 있어 대상을 밝힌다.
   */
  goldenCurvePrev: '이전 시간대 보기',
  goldenCurveNext: '다음 시간대 보기',
  /**
   * `goldenStart == goldenEnd` — **구간이 아니라 한 시각이다** (#200).
   *
   * 그날 남은 시간대가 한 칸뿐이면 서버가 시작과 끝을 같은 값으로 준다 (dev 22:12 KST 에
   * `23:00 – 23:00` 으로 관측). 대시로 이으면 0분짜리 구간처럼 읽혀 고장으로 보인다.
   *
   * `{time}` 치환. **시각 하나만 말하고 길이를 지어내지 않는다** — 예보 단위가 1시간이라
   * "23:00 – 24:00" 으로 늘리면 서버가 주지 않은 구간을 화면이 만든 것이 된다.
   */
  goldenSingleHour: '{time}쯤이 그중 나아요',
  /**
   * 창 안의 등급 분포를 말하는 문장 ([#637](https://github.com/8llow8llowMe/hondigagae/issues/637)).
   *
   * **예전에는 헤드라인 옆 배지였다** — `11:00 – 23:00 [주의]`. 그 배지는 창 **전체**의
   * 등급으로 읽혀서 "좋은 시간이라면서 주의?" 라는 모순을 만들었는데, 실제로 주의는
   * 창 안의 두 칸(14 · 15시)이었다. 배지가 가진 정보는 원래 **어디가 주의인가** 였고
   * 배지에는 그것을 적을 자리가 없다.
   *
   * **`{level}` 에 FE 가 한국어를 쓰지 않는다.** 서버 `walkSafetyLevel.name` 이 그대로
   * 들어간다 — `주의` 라는 글자가 이 파일에 있으면 서버가 등급 이름을 고쳐도 화면은
   * 옛 이름으로 말한다 (`frontend/CLAUDE.md` enum 규칙).
   */
  goldenAllSafe: '이 시간대는 내내 {level} 등급이에요',
  /**
   * 창 안에 안전이 아닌 구간이 있을 때. `{runs}` 는 그 구간들, `{pavement}` 는 그중
   * 노면 최고온도(정수), `{level}` 은 가장 심한 등급의 서버 `name` 이다.
   *
   * **노면온도를 근거로 적는 것은 지금 서버가 그 하나로 등급을 매기기 때문이다**
   * (`HourlyWalkSafetyItem` 주석). 서버가 시각 단위 `reasons` 를 주기 시작하면 이 문장을
   * 그 근거로 바꾼다 (골든타임-문구-세부명세 D8-1).
   */
  goldenCautionRuns: '{runs}는 노면이 {pavement}℃까지 올라 {level} 등급이에요.',
  /**
   * 위 문장 뒤에 이어 붙인다. **안전 구간이 없으면 붙이지 않는다** — 없는 위안을 만들지
   * 않는다. 두 문장이 390px 에서 세 줄을 넘으면 이쪽을 뺀다 (곡선이 좋은 구간을 면으로
   * 이미 보여 준다 — 홈-세부명세 D4-1-c).
   */
  goldenBetterRuns: ' {runs}가 좋아요.',
  /** `{from}`~`{to}` 시각 구간 표기. 한 시각이면 `goldenHourSingle` */
  goldenHourRange: '{from}–{to}시',
  /** 한 시각짜리 구간. `9–9시` 로 적으면 0분 구간처럼 읽힌다 (#200 과 같은 판단) */
  goldenHourSingle: '{from}시',
  /** 구간 사이 구분자 */
  goldenRunsJoin: ' · ',
  /**
   * 남은 예보가 아예 없는 날 (#204). 늦은 밤이면 오늘 남은 시간대가 없다.
   *
   * **`goldenNone` 과 다른 상태다.** 저쪽은 "남은 시간이 전부 위험" 이라는 **판정**이고
   * 이쪽은 판정할 **근거가 없다**는 뜻이다. 둘을 같은 문구로 묶으면 데이터가 0건인 날에
   * 화면이 위험을 단정한다 — 모르는 것과 나쁜 것을 구분하는 것이 이 서비스의 규칙이다
   * (루트 `CLAUDE.md`, `regional-weather-section.tsx` `RegionRow`).
   */
  goldenNoForecast: '오늘 남은 예보가 없어요',
  goldenNoForecastDesc: '남은 시간대 예보가 없어 오늘 판정을 만들 수 없어요.',

  // ── 제주 권역 날씨 비교 (#158) ─────────────────────────────────────────────
  regionHeading: '오늘 나가기 좋은 권역',
  /** `{name}` 을 권역 이름으로 치환한다. **단독 1위일 때만 쓴다** (#638) */
  regionRecommended: '오늘은 {name}이 가장 나아요',
  /**
   * 최고점이 2~4곳 동점인 날 (#638). `{names}` 는 ` · ` 로 이은 권역 이름들이다.
   *
   * **동점인데 한 곳을 고르면 화면이 표와 다른 말을 한다.** 2026-09-15 실측에서 다섯 배지가
   * 전부 `100` 인데 문장은 "제주시권이 가장 나아요" 였다 — 사용자는 그 근거를 표에서 찾지
   * 못하고 자기가 잘못 읽었다고 생각한다.
   *
   * **바꾸는 것은 "어디가" 뿐이다.** 서버 `recommendationReasons` 는 "왜 좋은가" 를 말하므로
   * 이 문장 아래에 그대로 붙는다.
   *
   * 조사는 항상 `이` 다 — 권역 이름이 전부 `권` 으로 끝난다(`제주시권이` · `서부권이`).
   * 서버가 그렇지 않은 `name` 을 내면 문장을 `{names} 모두 좋아요` 로 바꾼다 (조사 없음).
   */
  regionTied: '오늘은 {names}이 똑같이 좋아요',
  /**
   * 점수 있는 권역 **전부**가 동점인 날 (#638).
   *
   * **이름을 다 나열하지 않는다.** 다섯을 늘어놓아도 고를 것이 없다는 뜻은 같은데 줄만 길어진다.
   * 예보를 못 받은 권역은 셈에서 빠지므로 "어느 권역이든" 이 그 권역까지 가리키지 않는다.
   */
  regionTiedAll: '오늘은 어느 권역이든 좋아요',
  /**
   * 추천이 없는 날. **"그나마 여기" 를 쓰지 않는다** — 특보 경보이거나 어느 권역도 예보를
   * 못 받은 날이고, 적합도 0점·산책 위험이라고 말하는 같은 서비스가 여기서만 나가라고 하면 안 된다.
   */
  regionNone: '오늘은 추천할 권역이 없어요',
  /**
   * 점수의 뜻과 만점 (#638). **`regionScoreNote`('날씨만 본 점수예요')를 되살렸다.**
   *
   * #342 가 걷으면서 "되살릴 때는 그 자리가 바뀌었는지 먼저 본다" 고 적어 두었는데,
   * **자리가 바뀌었다** — #342 의 전제는 "배지가 날씨 값 바로 옆에 서 있어 자리가 말한다"
   * 였지만 #342·#530 을 거치며 값 묶음이 세로로 쌓이면서 배지가 숫자 아래가 아니라
   * 숫자 **오른쪽 끝**(`ml-auto`)에 홀로 서게 됐다. 붙어 있던 것이 떨어지면 자리는 말을 못 한다.
   *
   * **자리를 카드 제목 아래로 올렸다.** 하단 보조 문구로 되돌리지 않는다 — 표 아래 한 줄은
   * 배지를 다 읽은 뒤에야 닿아서, 무엇의 100인지 모르는 채로 다섯 칸을 훑게 된다.
   *
   * **만점을 함께 적는 이유는 배지가 `/100` 을 담을 자리가 없기 때문이다.** 장소 적합도는
   * `90 /100`(`MetricValue`)로 쓰는데 권역 배지는 `size="score"` 한 덩어리다. 같은 카드
   * 안에 만점이 적혀 있으면 배지마다 `/100` 을 되풀이할 이유가 없다.
   */
  regionScoreCaption: '반려견 활동 적합도 · 100점 만점',
  /**
   * 점수 배지 (#638). `{score}` 를 `weatherScore` 로 치환한다.
   *
   * **숫자만 두면 무엇의 100인지 알 수 없다.** 만점은 카드 캡션이 말하고 여기서는 단위만 붙인다 —
   * 배지가 담는 값이 개수도 순위도 아닌 **점수**라는 것을 배지 스스로 말하게 하는 한 글자다.
   */
  regionScoreUnit: '{score}점',
  /**
   * 권역 행의 온도 접두 (#206). 서버 `maxTemperature` 다.
   *
   * **숫자만 두면 무슨 온도인지 알 수 없다.** 바로 위 추천 문장은 서버가 준 완성형이라
   * "최고기온 26도, 강수확률 20% 로 …" 라고 정확히 말하는데, 표에서는 라벨이 빠져 있어
   * 같은 카드 안에서 한쪽만 라벨이 있었다. `minTemperature` 와 값이 같은 날이 많아
   * 드러나지 않았을 뿐이다.
   *
   * **낱말을 줄여 `최고` 만 쓴다** — 375px 에서 `제주시권 · 온도 · 강수 · 점수` 네 값이
   * 한 줄에 들어가야 한다.
   */
  regionTempPrefix: '최고',
  /**
   * 권역 행의 최저기온 접두 (#352). 서버 `minTemperature` 다.
   *
   * **`최고` 와 짝이라 같은 길이로 줄인다.** 두 줄이 위아래로 서므로 접두가 길이가 다르면
   * 숫자의 왼쪽 끝이 어긋난다 (`tabular-nums` 는 숫자만 맞춘다).
   *
   * **최저기온을 함께 두는 이유는 하루의 폭이다.** 최고만 있으면 31.0℃ 한 값으로 읽히는데,
   * 아침 산책을 계획하는 사람에게는 그 값이 몇 시의 이야기인지가 판단을 가른다.
   */
  regionMinTempPrefix: '최저',
  /** 예보를 못 받은 권역. **0점이 아니다** */
  regionScoreUnavailable: '예보 없음',
  /**
   * 2단 권역 레일의 좌우 이동 (#342).
   *
   * **`goldenCurve*` 를 재사용하지 않는다** — 같은 홈에 레일이 둘이고, 스크린리더는
   * 두 버튼을 같은 이름으로 읽어 어느 줄을 미는지 구분할 수 없다.
   */
  regionRailPrev: '이전 권역 보기',
  regionRailNext: '다음 권역 보기',
  /**
   * `{name}` 을 반려견 이름으로 치환한다.
   *
   * **"맞는 곳" 이라고 쓰지 않는다.** `pickTopPlaces` 는 목록의 앞 세 개를 그대로 집는다
   * (`places.slice(0, 3)`) — 적합도로 고른 것이 아니라 **고른 뒤에 적합도를 잰 것**이다.
   * 그래서 이 자리에 동반 불가·`0/100` 장소가 첫째로 설 수 있고, 실제로 그렇게 나갔다.
   *
   * 제목이 "맞는 곳" 이면 사용자는 그것을 **서비스의 판정**으로 읽는다. 그 약속을 지킬 수
   * 없으면 약속을 바꾸는 쪽이 맞다 — 화면이 못 지킬 말을 하면 사용자는 추천이 이상하다고
   * 읽는 것이 아니라 **판정 전체를 믿지 않게 된다.**
   *
   * "확인해 볼 곳" 은 이 섹션이 실제로 하는 일이다: 후보를 보여 주고 오늘 조건에서 어떤지
   * 함께 적는다. 고르는 일은 사용자 몫으로 남는다.
   *
   * 전체 정렬을 하려면 목록 전체에 적합도를 불러야 하는데 그것이 N+1 이라 못 한다 (D8-1).
   * **BE 가 정렬된 홈 요약을 주면**(공통명세 S6-1) 그때 이 문구를 되돌린다.
   *
   * **`어떨지 확인해 볼 곳` → `갈 만한 곳` 으로 줄였다** (#393). 약속의 세기는 그대로 두고
   * 길이만 줄인 것이다 — 바로 아래 `suitabilityFallback`(게스트)이 이미 `오늘 갈 만한 곳`
   * 이라 **같은 섹션의 두 갈래가 서로 다른 어법으로 말하고 있었다.**
   *
   * **`추천 여행지` 로 바꾸지 않는다.** 위 문단이 `맞는 곳` 을 기각한 근거가 그대로 적용된다 —
   * `pickTopPlaces` 는 앞 세 개를 그대로 집으므로 `0/100` · `판단 근거 부족` 장소가 첫째로
   * 설 수 있고, 실제로 그렇게 나간다. 그것을 `추천` 이라고 부르면 화면이 못 지킬 말을 한다.
   *
   * **`{name}` 에는 동반격 조사를 붙여 넣는다 — `withCompanionParticle()`.** 조사를 `과` 로
   * 박아 두었더니 dev 에서 "오늘 갱얼쥐**과** 갈 만한 곳" 이 나갔다. 받침 없는 이름에는
   * `와` 다. 같은 자리를 `src/lib/ai-plan/draft-title.ts` 는 이미 헬퍼로 짓고 있었다.
   */
  suitabilityHeading: '오늘 {name} 갈 만한 곳',
  suitabilityFallback: '오늘 갈 만한 곳',
  /**
   * 무엇을 반영해 적은 값인지 (`appliedFactorsOf`).
   *
   * **`반영` 은 실제로 반영됐을 때만 쓴다.** 예전에는 `'오늘 날씨와 혼잡도 반영'` 이 고정
   * 문구였는데, 그 바로 아래 장소 세 장이 모두 `혼잡도 정보 없음` 배지를 달고 서 있었다 —
   * 같은 눈높이에서 캡션과 배지가 서로를 부정한다.
   *
   * 응답이 이미 답을 갖고 있다: `weatherApplied` · `congestionApplied` 는
   * *"그 장소에 연결된 예측 데이터가 있었는가"* 다. 화면이 짐작하지 않고 그것을 읽는다 —
   * 골든타임이 `forecastCoverage` 를 그대로 말하는 것과 같은 축이다.
   *
   * **둘 다 없으면 `null` 이라 캡션 자체를 그리지 않는다.** "아무것도 반영 안 됨" 이라고
   * 적는 것은 근거가 아니라 변명이고, 그 사실은 장소마다 붙는 배지가 이미 말한다.
   */
  sortNote: {
    weatherCongestion: '오늘 날씨와 혼잡도 반영',
    weather: '오늘 날씨 반영',
    congestion: '오늘 혼잡도 반영',
    none: null,
  },
  /** 모바일 섹션 헤더는 폭이 좁아 짧게 쓴다 */
  sortNoteShort: {
    weatherCongestion: '날씨·혼잡도 반영',
    weather: '날씨 반영',
    congestion: '혼잡도 반영',
    none: null,
  },
  moreReasons: '근거 {n}개 더 보기',
  lessReasons: '근거 접기',
  emptyPlacesTitle: '표시할 장소가 없어요',
  emptyPlacesDesc: '장소를 찾아보세요.',
  findPlaces: '장소 찾기',
  upcomingHeading: '다가오는 일정',
  /**
   * 이 섹션에 선 일정이 **오늘 여행 중**일 때의 제목 (#561). 같은 섹션이고 제목만 바뀐다 —
   * 홈이 한 건만 세우기 때문에 가능한 표기다 (`home-view` `showsOngoingPlan` 주석).
   */
  ongoingHeading: '진행 중인 일정',
  noPlanTitle: '아직 일정이 없어요',
  noPlanDesc: '반려견에게 맞는 일정을 만들어보세요.',
  /**
   * 일정은 있는데 **전부 지나간** 경우. `noPlanTitle` 과 갈라 둔다 —
   * 넷 다 지난 계정에 "아직 일정이 없어요" 라고 말하면 사용자는 자기 일정이 사라졌다고
   * 읽는다. 다음 행동도 다르다: 저쪽은 만들기 유도, 이쪽은 목록으로 보내는 안내다.
   */
  noUpcomingPlanTitle: '다가오는 일정이 없어요',
  noUpcomingPlanDesc: '지난 일정은 일정 목록에서 볼 수 있어요.',
  emergencyTitle: '주변 동물병원 찾기',
  /**
   * **개수를 단정하지 않는다** (#206).
   *
   * 백엔드가 이 수를 주지 않아 예전에는 `제주 24시간 병원은 3곳뿐이에요` 로 하드코딩해
   * 뒀다(공통명세 S5-3). 그런데 배너를 눌러 들어간 화면은 실제 값을 쓰므로, 데이터가
   * 어긋나는 순간 **홈이 "3곳" 이라고 말하고 목록은 "반경 안에 없어요" 라고 말한다** —
   * dev 에서 실제로 그랬다.
   *
   * 이 배너가 전할 것은 "적으니 미리 확인해 두라" 는 **동기**이고, 그것은 수를 세지
   * 않고도 말할 수 있다. 정확한 수는 다음 화면이 센다.
   */
  emergencyDesc: '24시간 진료가 확인된 곳은 많지 않아요',
  /**
   * 실제 시설을 찾았을 때의 배너 설명. `{name}` · `{distance}` 치환.
   *
   * **위 `emergencyDesc` 의 규칙을 어기는 것이 아니다.** #206 이 금지한 것은 FE 가
   * **수를 적어 두는 것**이었다 — 홈이 "3곳" 이라고 박아 두고 목록은 `/emergencies/facilities`
   * 를 부르니 둘이 어긋났다. 이 문구들은 **그 목록과 같은 API 의 응답**을 그대로 말하므로
   * 어긋날 자리가 없다. 개수도 세지 않는다 (`pickNearestHospital` 머리주석).
   *
   * 좌표를 못 얻었거나 조회 전·실패면 `emergencyDesc` 로 남는다 — 배너는 어떤 상태에서도
   * 사라지지 않는다 (`components/banner.tsx`).
   */
  emergencyNearest24h: '24시간 · {name} {distance}',
  /** 24시간은 없고 지금 문이 열린 곳은 있을 때 */
  emergencyNearestOpen: '지금 진료중 · {name} {distance}',
  /**
   * 열림 여부를 말하지 않는 갈래. **`openNow === null` 이 여기로 온다** — null 은 "닫힘"
   * 이 아니라 "영업시간 정보가 없어 판정할 수 없음" 이라, 배너가 열림/닫힘을 단정하지 않고
   * 위치만 말한다 (`types/emergency.ts` `openNow`).
   */
  emergencyNearest: '가장 가까운 곳 · {name} {distance}',
  /**
   * 실내 대안의 거리 (`AlternativePlaceItem.distanceMeters`). `{distance}` 치환.
   *
   * **`직선` 을 반드시 붙인다.** 서버가 하버사인으로 재므로 직선거리이고, 제주는 산간·
   * 해안도로가 많아 주행거리와 크게 다르다 — 일정 상세의 `plan.distanceStraight` 와 같은
   * 이유이고 같은 표기다.
   */
  indoorDistance: '직선 {distance}',
  /**
   * 실내 대안 섹션의 기준을 밝힌다.
   *
   * **거리의 기준점을 말하지 않으면 안 된다.** 이 값은 "적합도를 조회한 장소로부터" 의
   * 거리인데, 홈은 여러 장소의 대안을 하나로 접으므로(`collectIndoorAlternatives`) 숫자만
   * 두면 무엇으로부터의 거리인지 알 수 없다. 장소 행이 거리를 안 그리는 이유와 같은
   * 판단이다 (`place-insight-row.tsx` `metaLine`).
   */
  indoorNote: '위 목록의 장소에서 가까운 실내예요',
  congestionUnknown: '혼잡도 정보 없음',
  /** 점수를 내지 못한 경우. **0점이 아니다** */
  scoreUnavailable: '판단 근거 부족',
  indoorHeading: '비 올 때 갈 만한 실내',
  /** 미로그인 안내 */
  guestVerdictNotice: '반려견을 등록하면 우리 아이 기준으로 판정해요',
  registerPet: '반려견 등록',
  /**
   * 프로필 태그는 짧은 표기를 쓴다 — 아트보드 `01 홈 · P1`.
   * `messages.pet.labels` 의 "더위에 민감해요" 는 문장형이라 태그로는 길다.
   */
  traitHeat: '더위 민감',
  traitCold: '추위 민감',
  traitNoise: '소리 민감',
  traitWalk: '산책 선호',
  /**
   * 미로그인 · 반려견 0마리일 때 프로필 자리 (아트보드 04-② · 홈-첫방문-판정-세부명세 D4).
   *
   * **지시가 아니라 이유로 바꿨다.** `반려견을 등록해 주세요` 는 시키는 말이라 왜
   * 해야 하는지가 제목에 없었고, 그 답은 바로 아래 설명에만 있었다. 제목이 얻는 것을
   * 말하면 설명이 그 얻는 것을 구체화한다 — 제목과 버튼(`registerPet`) 사이에 이유가 선다.
   *
   * 설명의 `더위 민감도` 는 `민감도` 보다 길지만 **화면이 실제로 쓰는 축의 이름**이다
   * (`traitHeat`). 무엇이 반영되는지가 예로 보여야 등록할 이유가 된다.
   */
  guestProfileTitle: '우리 아이 기준으로 보려면',
  guestProfileDesc: '크기·더위 민감도까지 넣은 판정을 볼 수 있어요',
  /**
   * 섹션 푸터. **여기도 "맞는 곳" 을 쓰지 않는다** — 제목이 약속을 거뒀는데 푸터가
   * 그대로면 같은 섹션이 두 가지 말을 한다 (`suitabilityHeading`).
   */
  /*
    **점수를 못 낸 곳** (#428). 목록 하단에 접힌 한 줄로 선다.

    "판단 근거 부족" 이라는 서버 등급어를 여기서 다시 쓰지 않는다 — 그 문구는 배지가
    쓰는 값이고, 이 줄은 배지가 아니라 **개수**를 말한다. 사용자가 알아야 하는 것은
    "왜 점수가 없나" 가 아니라 "몇 곳이 빠졌나" 다.
  */
  unscoredPlaces: '점수를 내지 못한 곳 {n}곳',
  morePlaces: '장소 {n}곳 더 보기',
  allPlaces: '장소 {n}곳 전체 보기 ›',
  allPlans: '일정 전체 보기 ›',
  /** 메타 줄 · 태그의 실내/야외 표기 */
  indoor: '실내',
  outdoor: '야외',
  verdictErrorTitle: '오늘 판정을 불러오지 못했어요.',
} as const
