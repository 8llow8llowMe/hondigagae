/**
 * 지도 뷰 문구.
 *
 * 아트보드 `혼디가개 장소 찾기` 05·06절, `혼디가개 긴급 시설` 02·04절.
 * **장소와 긴급 시설이 같은 전환을 쓰므로 문구도 한 곳에 둔다** — 두 화면에서
 * "지도" / "지도로 보기" 로 갈리면 같은 컨트롤이 다르게 보인다.
 */
export const mapMessages = {
  // ── 보기 전환 ──────────────────────────────────────────────────────────

  /** 세그먼트 컨트롤. 데스크톱은 글자, 모바일 지도 화면은 아이콘 + aria-label */
  viewToggleLabel: '보기 방식',
  listView: '목록',
  mapView: '지도',
  showList: '목록으로 보기',
  showMap: '지도로 보기',

  // ── 지도 상태 ──────────────────────────────────────────────────────────

  loading: '지도를 불러오는 중',

  /**
   * **SDK 실패는 종류별로 다르게 말한다.** 키가 없는 것은 우리 설정 문제라
   * 사용자가 할 수 있는 일이 없고, 네트워크 실패는 다시 시도할 수 있다.
   * 어느 쪽이든 **목록으로 되돌린 뒤** 이 줄을 상단에 둔다 (아트보드 05 마지막 단락).
   */
  errorNoKey: '지도를 준비 중이에요. 목록으로 보여드릴게요.',
  errorScript: '지도를 불러오지 못했어요. 목록으로 보여드릴게요.',
  errorUnsupported: '이 브라우저에서는 지도를 쓸 수 없어요. 목록으로 보여드릴게요.',
  retry: '지도 다시 불러오기',

  // ── 목록 ↔ 지도 ────────────────────────────────────────────────────────

  /**
   * `{n}` 치환 — 지도 영역 안에 든 개수.
   *
   * **제목이 아니라 캡션이다** (#294 후속). 패널 머리에 "지도에 보이는 곳 20" 을 제목으로
   * 두면, 제목이 할 일이 없는 자리를 차지한다 — 패널이 무엇인지는 안에 든 목록이 이미
   * 말한다. 그 자리는 필터가 쓰고, 개수는 목록 바로 위 한 줄로 내렸다.
   */
  visibleCount: '지도에 보이는 {n}곳',
  /**
   * `{n}` 치환 — 묶음 마커의 **접근성 이름**이다. 누르면 그 구역으로 확대한다.
   *
   * **눈에 보이는 글자가 아니다** (#655). 마커는 지름 32 원 안에 숫자만 쓰고
   * (`lib/map/cluster.ts` · 진단 E-1), 이 문구는 `aria-label` 로 간다 — 원 안에
   * 넣으면 폭이 글자 수를 따라 늘어 390px 에서 마커끼리 겹친다.
   */
  clusterCount: '이 지역 {n}곳',

  emptyInView: '이 지역에는 표시할 곳이 없어요',
  emptyInViewDescription: '지도를 움직이거나 조건을 풀어 보세요.',

  // ── 패널 · 시트 ────────────────────────────────────────────────────────

  /** 지도를 현재 위치로 옮긴다. **제주 안일 때만 버튼이 있다** */
  myLocation: '내 위치로 이동',

  collapsePanel: '목록 패널 접기',
  expandPanel: '목록 패널 펼치기',
  expandSheet: '목록 더 보기',
  collapseSheet: '목록 접기',
  sheetHint: '시트를 올리면 전체 목록을 봐요.',

  // ── 액션 ───────────────────────────────────────────────────────────────

  /** 경로 안내를 우리가 그리지 않는다 — 외부 지도 앱으로 넘긴다 */
  directions: '길찾기',
  /** 새 탭으로 열리는 것을 미리 알린다 */
  directionsHint: '카카오맵에서 열려요',

  /**
   * 지도 패널 행에서 장소 상세로. **선택 버튼 밖**에 둔다 — `<button>` 안에 링크를
   * 넣을 수 없다. 행을 누르는 것은 "이동" 이 아니라 "이 핀 고르기" 라 행 전체를 링크로
   * 만들 수도 없어서, 상세로 가는 길만 액션 열로 내보냈다.
   */
  rowDetail: '상세',
  /** 한 화면에 여러 개라 보이는 글자만으로는 전부 "상세" 로 읽힌다 */
  rowDetailLabel: '{title} 상세 보기',

  /** 좌표가 없는 곳은 마커를 그리지 않는다. 그 사실을 목록에서 말한다 */
  noCoordinate: '위치 정보가 없어 지도에 표시할 수 없어요',
} as const
