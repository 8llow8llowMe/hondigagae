/**
 * 제주올레 코스 (#618 · 이름과 경로는 #810).
 *
 * **서버가 내려주는 문구는 여기 없다** — `providerName` · `baseDate` · 404 `resultMessage`
 * 는 그대로 렌더한다 (`docs/api-integration-guide.md` §6). 활동량 이름(`낮음`·`보통`)도
 * 여기 없다: 기준 줄은 응답의 `appliedPetActivityLevel.level.name` 을 쓰고, 세그먼트
 * 선택지는 등록 폼과 같은 `messages.pet.options.activityLevel` 을 쓴다
 * (공통명세 S4-1 규칙 6 · `api-integration-guide.md` §6 "서버가 값을 주지 않는 컨트롤").
 *
 * **상한 숫자(4시간·6시간)도 여기 없다** (#735). 응답의 `maxDurationMinutes` 를
 * `formatDuration` 으로 옮겨 적는다 — 화면이 상한을 제 상수로 적으면 서버가 상한을 바꾼 날
 * 화면만 옛 숫자를 말한다.
 *
 * **제목에는 마침표를 찍지 않고 설명에는 찍는다** — `/places` · `/favorites` 와 같다.
 */
export const walkCourseMessages = {
  /**
   * **`산책 코스` 가 아니다** ([#810](https://github.com/8llow8llowMe/hondigagae/issues/810)).
   * 적재된 29개는 중앙값 15.7km · 23개가 5시간 이상인 **제주올레 종주 코스**다. 반려견
   * 동반 기준으로 이것을 `산책` 이라 부르는 것은 29개 중 28개에 대해 사실과 다르고,
   * 홈 배너(`bannerTitle`)가 이미 `제주올레` 라 **누른 이름과 닿은 이름이 갈렸다**
   * (진단 문서 W-2 · §4-1 ①안). 경로도 같은 이유로 `/olle` 다.
   */
  pageTitle: '제주올레 코스',
  /**
   * 부제는 **이 화면이 반려견 여행 설계의 일부라는 것**을 제목에 잇는다 (§4-1).
   *
   * **대상어(`제주올레 코스`)를 문장에서 빼지 않는다.** 이 상수는 목록 부제 자리 하나가
   * 아니라 **404 `EmptyState` · 형식 오류(400) 화면 · 상세 `meta description`** 에서도
   * 쓰인다 (`walk-course-detail-section.tsx` · `olle/[walkCourseId]/page.tsx`). 제목이
   * 곁에 없는 그 자리들에서 `구간을 골라 봐요` 만 남으면 **무엇을 고르라는지 말하지 않는다.**
   *
   * 시간 축의 이름은 `걷는 시간` 으로 통일돼 있다 (§4-2 · D-1) — `소요시간` 과
   * 섞어 쓰지 않는다.
   */
  pageDescription: '우리 아이랑 걸을 만한 제주올레 코스를 거리와 걷는 시간으로 골라 봐요.',

  // ── 홈 진입점 (공통명세 S6-1) ─────────────────────────────────────────────
  /**
   * **전역 nav 에 넣지 않는다.** nav 셋(장소 찾기·여행 일정·AI 일정 생성)은 *할 일* 축이고
   * 항목을 늘리지 않기로 이미 정해져 있다 (`nav-links.tsx`). `Banner` 는 §0 이 인정한
   * 상시 진입점이다.
   */
  bannerTitle: '제주올레 걸어 보기',
  /** **개수를 적지 않는다.** 코스 수는 서버가 세고 적재(#383)로 바뀐다 */
  bannerDescription: '거리와 걷는 시간으로 걸을 만한 코스를 골라 봐요',

  // ── 목록 ──────────────────────────────────────────────────────────────────
  /** `{count}` 치환. `aria-live` 로 알리는 줄이기도 하다 (D6) */
  listCount: '코스 {count}개',
  /**
   * 활동량 축의 이름. **라디오그룹의 접근 이름이자 세그먼트 위 보이는 캡션이다** (#734).
   *
   * 축 이름이 `걷는 시간` 에서 `활동량` 으로 바뀌었다 (#735): 선택지가 상한 시간
   * (`4시간 이내`)이 아니라 활동량 이름(`낮음`·`보통`)이 되었기 때문이다. 상한은 화면이
   * 적을 수 없는 서버 값이라, 고른 뒤 **기준 줄**이 응답 값으로 말한다.
   *
   * 두 자리가 같은 문구라 캡션은 `aria-hidden` 이다 — 두 번 읽히지 않게 한다.
   */
  activityGroupLabel: '활동량',
  activityAll: '전체',
  sortGroupLabel: '정렬',
  sortCourseNo: '코스 순',
  /**
   * **무엇이 짧은지 말한다** ([#798](https://github.com/8llow8llowMe/hondigagae/issues/798)).
   * 값은 `DISTANCE_ASC` 인데 같은 행에 소요시간도 있어, `짧은 순` 만으로는 "짧은 시간 순"
   * 으로 읽힌다.
   */
  sortDistanceAsc: '거리 짧은 순',
  /** 1024 이상 열 머리의 코스 열 라벨 (#734 · #797). 거리·소요시간·시종점은 상세와 같은 라벨(아래)을 그대로 쓴다 */
  columnCourseLabel: '코스',

  /**
   * 기준 줄. **`appliedPetActivityLevel` 이 있을 때만 그린다** — 로컬 상태가 아니라
   * 응답을 믿는다 (공통명세 S4-1 규칙 5).
   *
   * `{pet}` · `{level}` · `{limit}` 치환. `{level}` 은 **서버 enum metadata 의 `name`**,
   * `{limit}` 은 **서버 `maxDurationMinutes`** 를 옮겨 적은 것이다 (`4시간`).
   *
   * **세그먼트가 잃은 숫자를 이 줄이 갖는다** (#735). 컨트롤은 `낮음`·`보통` 만 말할 수
   * 있고(상한은 응답에만 있다), 그 활동량이 몇 시간인지는 고른 뒤 여기서 드러난다.
   *
   * ### 보여 준 코스가 아니라 **거른 기준**을 말한다 (#776 · 명세 D5-3)
   *
   * 전에는 `{limit} 이내 코스만 보여 줘요` 였다. 그것은 **보여 준 코스 전부**에 대한
   * 단정인데 서버는 그것을 보장하지 않는다 — `WalkCourseActivityFit.fits` 는
   * `durationMaxMinutes == null` 이면 곧바로 참이라 **소요시간을 모르는 코스가 어느
   * 활동량에서도 걸러지지 않는다.** 그 규칙 자체는 맞고 바꾸지 않는다(모르는 것을 나쁜
   * 것으로 판정해 지우면 사용자는 그 코스가 있다는 것조차 모른다). 바꾼 것은 문장이다.
   *
   * 지금 문장은 `fits` 를 그대로 옮긴 것이다: 서버는 **넘는다고 아는 것만** 뺀다. 미상
   * 코스는 "넘는 코스" 가 아니므로 섞여 나와도 이 줄은 참이다.
   *
   * **dev 실측(2026-09-21)은 29개 전부 파싱돼 `null` 이 0건이었다** — 오늘은 옛 단정도
   * 우연히 참이었다. 고친 이유는 지금 거짓이어서가 아니라, 적재(#722·#383)가 파싱 실패
   * 코스를 하나 들여오는 순간 **아무도 이 줄을 다시 보지 않기 때문**이다.
   *
   * `{limit}` 은 `formatDuration` 결과라 언제나 `분`·`시간` 으로 끝난다 — 조사는 `이` 고정.
   */
  activityBasis: '{pet} 활동량({level}) 기준으로 {limit}이 넘는 코스는 빼고 보여 줘요.',
  /**
   * 반려견 이름을 붙이지 않는 기준 줄 — 사용자가 세그먼트로 **직접 고른** 경우와, 대표견
   * 조회가 실패했는데 URL 로 조건을 들고 들어온 경우다. **없는 이름을 지어내지 않는다.**
   *
   * 활동량 이름은 남는다 — 그것은 반려견 프로필이 아니라 **응답이 준 값**이라 지어낸 것이
   * 아니고, 이 줄의 요점(`보통` 이 곧 `6시간`)이 거기 있다.
   *
   * 이름만 빠질 뿐 **약속의 세기는 위와 같다** (#776) — 두 문장이 갈리면 반려견 이름이
   * 있고 없고에 따라 화면이 다른 것을 약속하게 된다.
   */
  activityBasisWithoutPet: '활동량({level}) 기준으로 {limit}이 넘는 코스는 빼고 보여 줘요.',

  emptyTitle: '조건에 맞는 코스가 없어요',
  emptyDescription: '걷는 시간을 넓혀 보세요.',
  emptyAction: '전체 코스 보기',

  errorTitle: '코스를 불러오지 못했어요',

  /**
   * **`LOW` 를 고르면 좌표 있는 코스가 0개가 된다** (D8-2 ②). 4개 모두 `4~5시간`·`5~6시간`
   * 이라 4시간 상한에서 전부 걸러진다 — 결함이 아니라 데이터 분포이고, 적재(#383) 후
   * 저절로 해소된다. 그 사실을 말하지 않으면 사용자는 기능의 존재를 모른다.
   */
  noGoldenInScope: '이 조건에는 날씨를 볼 수 있는 코스가 아직 없어요.',

  // ── 상세 ──────────────────────────────────────────────────────────────────
  distanceLabel: '거리',
  durationLabel: '소요시간',
  startEndLabel: '시종점',
  backToList: '코스 목록으로',
  /** `{provider}` · `{date}` 치환 — **둘 다 서버 값이다** */
  source: '{provider} · {date} 기준',

  /** 404 에서 서버 `resultMessage` 가 비었을 때만 쓰는 대체 문구 */
  detailNotFoundTitle: '없는 코스예요',

  /**
   * 좌표가 없는 코스 — **안내 상자의 첫 줄**([#730](https://github.com/8llow8llowMe/hondigagae/issues/730)).
   *
   * **"아직" 이라고 말한다** — 데이터가 채워지면 생기는 값이라, "정보가 없어요" 는
   * 영구적 결핍처럼 들린다.
   *
   * D5-2 는 이 문장 **하나만** 남기고 섹션을 걷으라고 적었는데, 그 갈래가 25/29 라
   * 화면의 기본형이 절반 빈 화면이 됐다 (#730 실측: 390 에서 문서 높이의 53%).
   * 지금은 섹션과 제목을 유지하고 이 문장이 상자의 첫 줄이 된다.
   */
  noCoordinates: '이 코스는 시작점 좌표가 아직 없어서 오늘 걷기 좋은 시간을 알려 드리지 못해요.',
  /**
   * ② 얼마나 흔한 일인지 (#730).
   *
   * **개수를 적지 않는다** — 코스 수도 좌표를 가진 코스 수도 서버가 세고 적재(#383)로
   * 바뀐다 (`bannerDescription` 과 같은 규칙). 사용자가 알아야 하는 것은 숫자가 아니라
   * **자기가 뭘 잘못 누른 것이 아니라는 사실**이다.
   */
  noCoordinatesCommon:
    '아직 좌표가 들어오지 않은 코스가 대부분이에요. 좌표가 채워지면 이 자리에 시간대별 판정이 생겨요.',
  /**
   * 남는 대안 하나 — 장소 찾기로 보낸다 (`/places`).
   *
   * **`일정에 담기` 안내는 #780 에서 지웠다.** 그 버튼이 같은 화면 바로 아래에 실물로
   * 있어 안내가 그것을 또 가리키면 같은 말이 두 번 나갔다.
   *
   * **검색어를 미리 채우지 않는다.** `startEndPoint` 는 `제주민속촌주차장 입구-남원포구`
   * 처럼 공백과 하이픈이 섞인 원문이고 갈라 쓰지 않기로 이미 정했다 (D4-2). 서버
   * `keyword` 는 `title`·`addr1` 의 `%LIKE%` 라(`PlaceCustomRepositoryImpl`) 그 원문도,
   * 갈라 만든 토막도 대부분 0건으로 떨어진다 — **결과 없음으로 데려가는 대안은 대안이 아니다.**
   */
  noCoordinatesPlacesAction: '시·종점 근처 장소 보기',
  noCoordinatesPlacesDescription:
    '위 시종점 이름으로 장소를 찾으면 그곳의 오늘 판정을 볼 수 있어요.',
  /** `다시 시도` 만이면 무엇을 다시 하는지 모른다 (D6) */
  goldenRetry: '날씨 다시 불러오기',
  /**
   * 시작점 지도 카드의 제목 ([#782](https://github.com/8llow8llowMe/hondigagae/issues/782)).
   *
   * **`코스 지도` 가 아니다.** 그리는 것은 시작점 마커 하나뿐이고 경로 좌표열은 계약에
   * 없다 — 제목이 코스 전체를 보여 준다고 약속하면 본문이 그것을 지키지 못한다.
   */
  startMapHeading: '코스 시작점',
  /**
   * 상세 브레드크럼 `<nav>` 의 접근 이름. **장소 상세와 같은 문자열을 쓴다**
   * (`messages.place.detailBreadcrumbLabel`) — 같은 일을 하는 자리가 화면마다 다른
   * 이름으로 들리면 보조기기 사용자가 둘을 다른 것으로 읽는다.
   *
   * `코스 경로` 라고 쓰지 않는다 — 이 화면에서 그 말은 **걷는 경로**로 읽힌다.
   */
  detailBreadcrumbLabel: '현재 위치',
  /**
   * 지도 SDK 를 못 불러왔을 때 제목 아래 한 줄 (#782).
   *
   * **제목이 약속한 것이 화면에 없으면 그 사실을 말한다.** `PlaceMiniMap` 은 제목이 없어
   * 조용히 강등되지만 이 카드는 `코스 시작점` 이라는 제목을 갖고 있어, 지도만 사라지면
   * 제목이 붕 뜬다. 오류로 읽히지 않게 **다음 행동(길찾기)을 가리키는 말**로 적는다.
   */
  startMapUnavailable: '지도를 불러오지 못했어요. 아래 길찾기로 위치를 확인할 수 있어요.',
} as const
