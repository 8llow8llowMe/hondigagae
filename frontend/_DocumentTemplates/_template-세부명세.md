# [화면명] 세부 명세

> 상위 공통명세: `docs/features/<feature>/...`
> 작성자 / 작성일:
> 계약 확인 근거 (호출한 Swagger URL + 확인 일시):

## D0. 화면 정의

- **경로**:
- **인증**: 필요 / 불필요 (`middleware.ts` `PROTECTED_PATHS` 등록 여부)
- **진입 경로**:
- **이탈 경로**:

## D1. 레이아웃

- 모바일 (375):
- 태블릿 (768):
- 데스크톱:

긴 한국어 실데이터로 확인할 항목:

## D2. 컴포넌트 구성

| 컴포넌트 | 신규/기존 | 위치 | 비고 |
| -------- | --------- | ---- | ---- |
|          |           |      |      |

- 재사용할 공통 컴포넌트 (`docs/styling-guide.md` §2):
- 새로 만들 공통 컴포넌트 (있으면 이유):

## D3. 데이터 흐름

```text
화면 진입 → [server/client] → API → 렌더
```

| 항목                   | 내용 |
| ---------------------- | ---- |
| API                    |      |
| React Query key        |      |
| staleTime / retry      |      |
| mutation 후 invalidate |      |
| 폴링 여부 / 종료 조건  |      |

## D4. 상호작용

| 트리거 | 동작 | 결과 |
| ------ | ---- | ---- |
|        |      |      |

## D5. 상태별 화면

| 상태                        | 표시                | 문구 (확정)                             |
| --------------------------- | ------------------- | --------------------------------------- |
| loading                     |                     | —                                       |
| success                     |                     |                                         |
| 데이터 부재 (404)           | EmptyState          | (서버 `resultMessage` 노출 + 다음 행동) |
| 일시 장애 (5xx)             | ErrorState + 재시도 |                                         |
| 부분 데이터 없음 (nullable) | **숨김**            | —                                       |

## D6. 접근성

- icon-only 버튼 `aria-label`:
- 키보드 순회:
- 터치 영역 44px:
- 색 외 정보 전달 수단:

## D7. 테스트 케이스

`_template-테스트-케이스.md` 로 분리하거나 여기에 요약한다.

- 순수 로직:
- 렌더 분기 (404 / 5xx 필수):
- 접근성 계약:

## D8. 미결 사항

| #   | 항목 | 선택지 | 추천 |
| --- | ---- | ------ | ---- |
| 1   |      |        |      |

## D9. BE 후속 요청 (있으면)

| #   | 요청 | 이유 |
| --- | ---- | ---- |
| 1   |      |      |
