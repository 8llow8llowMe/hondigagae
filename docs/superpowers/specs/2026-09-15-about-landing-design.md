# 서비스 소개 페이지 — `/about` 확장 설계

> 대상: `app/(main)/about/page.tsx` · `features/about/about-view.tsx` · `lib/messages/about.ts` · `DESIGN.md`(표면 예외 1절 추가)
> 이슈: [#635](https://github.com/8llow8llowMe/hondigagae/issues/635)
> 작성: 2026-09-15 (모션 §6-4 같은 날 추가)
> 상태: **구현 완료** (#635, 2026-09-15) — 계획 `docs/superpowers/plans/2026-09-15-about-landing.md`, 세부명세 `frontend/docs/features/about/소개페이지-세부명세.md`
> 목업: `frontend/docs/features/about/소개페이지-목업.html` (정적 HTML 1장, 브라우저로 연다)

**Goal:** 로그인 없이 열리는 `/about` 을 **서비스 소개 페이지**로 확장한다. 첫 방문 보호자가
"이 서비스가 내 질문에 답하는가" 를 한 화면에서 판단하고 홈으로 넘어가게 하고, 공모전
심사자가 마지막 절에서 데이터 활용의 근거를 확인하게 한다.

**Non-goal:** 홈(`/`)의 역할 변경 — 홈은 이미 미로그인 방문자에게 실제 판정을 먼저 보여 주는
화면이다(홈-세부명세 D5-3). 새 라우트. 백엔드 호출. 회원가입·소셜 로그인·마이페이지 소개.
산책 코스(올레 29) 소개 — BE 진행 중이라 화면이 없다(screen-inventory §6-1). AI 후보 기능
10종 중 미선정 기능의 소개.

---

## 1. 진단

### 1-1. 서비스를 설명하는 화면이 없다

`/about` 은 #611 에서 **모바일 푸터가 빠지며 출처 표기가 갈 곳**으로 생겼다. 소개 문단 3줄 ·
데이터 출처 5개 · 면책 1줄, 정적 카드 셋이다. 서비스가 무엇을 판단하고 왜 믿을 수 있는지는
어디에도 없다.

홈은 반대로 **설명 없이 판정부터** 보여 준다. 그 설계는 옳다 — 첫 방문자가 데이터 판정을
먼저 보고 가입하게 한다(D5-3). 다만 홈은 "오늘 제주시" 라는 한 시점의 답이라 서비스의
범위(장소 필터 · 일정 · AI · 긴급)를 말하지 못한다.

### 1-2. 두 청중이 같은 문을 쓴다

| 청중 | 들어오는 길 | 알고 싶은 것 |
| --- | --- | --- |
| 첫 방문 보호자 | 검색 · 공유 링크 · 홈 하단 "서비스 소개" | 내 개를 데려가도 되는지, 지금 나가도 되는지 |
| 공모전 심사자 · 관광 담당자 | 출품 자료의 URL, 모바일 비로그인 | 어떤 공공데이터를 어떻게 판단으로 바꿨나 |

**두 청중은 순서가 다르다.** 보호자는 자기 질문의 답이 보이면 읽고, 심사자는 데이터가
보이면 읽는다. 한 화면에서 둘을 다 잡으려면 **앞은 보호자, 뒤는 심사자**로 갈라야 하고,
사용자가 정한 우선순위는 **보호자 우선**이다.

### 1-3. 소개 페이지가 홈과 경쟁하면 진다

소개가 홈의 판정 카드를 그대로 다시 그리면 같은 사실을 두 화면이 말한다(DESIGN.md §1
"같은 사실을 두 번 말하지 않는다"). 소개는 **홈으로 보내는 화면**이다 — 실데이터를 부르지
않고 **고정 예시**로 "이런 판정을 한다" 만 보여 주고, 실제 값은 홈이 답한다.

---

## 2. 페르소나

| # | 페르소나 | 상황 | 불안 | 답이 되는 기능 | 우선 |
| --- | --- | --- | --- | --- | --- |
| P1 | **첫 동반 여행 보호자** | 30대, 소형~중형견 1마리, 여름 제주 2박 3일. 출발 전 검색 중 | 데려가도 되는 곳인지 · 더운데 발바닥은 괜찮은지 · 일정을 어떻게 짜나 | 내 반려견 기준 필터 · 산책 판정 · AI 일정 | **1** |
| P2 | **여행 중 보호자** | 이미 제주. 모바일, 이동 중 | 오늘 언제 나가나 · 비 오면 어디로 · 다치면 어디로 | 골든타임 · 실내 대안 · 한산한 날 · 긴급 시설 | 2 |
| P3 | **심사자 · 관광 담당자** | 출품 자료 링크로 진입, 비로그인 | 데이터를 어떻게 결합했나 · 근거가 투명한가 · 규모 | 근거 원칙 · 5원천 · 315/214 · 아키텍처 한 줄 | 3 (마지막 절) |

P1 과 P2 는 같은 사람의 두 시점이다. 절 구성이 **질문 순서**를 따르면 둘이 같은 화면에서
각자 자기 절을 찾는다.

---

## 3. 무엇을 강조하고 무엇을 뺐나

### 3-1. 강조 — 이 서비스만 하는 말부터

| 순위 | 기능 | 왜 이 자리인가 |
| --- | --- | --- |
| 1 | **산책 안전 판정** (추정 노면 온도 · 체감온도 · 골든타임) | 기온이 아니라 **발바닥이 닿는 온도**로 판정한다. 브랜드 심볼(발바닥)의 존재 이유이고, 다른 여행 서비스에 없다. 히어로 예시가 이것이다 |
| 2 | **내 반려견 기준 필터** (크기 · 체중, 315곳) | "데려가도 되나" 가 P1 의 첫 질문. 정보 없음을 배제하지 않는 규칙까지 신뢰 요소다 |
| 3 | **적합도 + 근거** · **한산한 날** | 점수만 주지 않고 이유를 붙인다(XAI). 30일 혼잡도에서 서버가 하루를 고른다 |
| 4 | **AI 일정 · 비 오면 실내 대안** | 실제 있는 장소 목록 안에서만 고른다(환각 차단). 하루만 다시 짜기 · 준비물까지 이어진다 |
| 5 | **긴급 시설 214곳** | P2 의 가장 급한 질문. 짧게, 그러나 빠지지 않게 |
| 6 | **데이터 원천 5곳 · 근거 원칙** | P3 몫. 기존 출처 카드가 이 절로 흡수된다 |

### 3-2. 뺀 것

| 뺀 것 | 이유 |
| --- | --- |
| 회원가입 · 소셜 로그인 · 마이페이지 · 저장한 장소 | 있어야 하는 기능이지 고를 이유가 아니다. CTA 가 "가입 없이 홈부터" 라 소개하면 모순이다 |
| 산책 코스(올레 29) | BE 진행 중, FE 화면 없음. 없는 것을 소개하지 않는다 |
| 미선정 AI 기능 | 루트 `CLAUDE.md` — 선정 전 기능은 구현하지 않는다. 소개도 같다 |
| 아키텍처 다이어그램 | 심사자용 한 줄 캡션으로 줄인다. 다이어그램은 `README.md` 에 있고, 이 화면은 제품이다 |
| 점수 숫자 예시 (`87/100`) | 홈-세부명세 D8-2 가 `scoreDelta` 숫자를 노출하지 않기로 했다. 예시도 등급어(`적합도 높음`)로 말한다 |

---

## 4. 구조 — 질문 3개

### 4-1. 채택: 보호자의 질문 순서로 절을 나눈다

```text
데려가도 돼요?  →  지금 나가도 돼요?  →  오늘 어디 가요?  →  위급하면?  →  무엇을 보고 판단하나요
```

기능 이름이 아니라 **사용자의 불안**이 절 제목이다. DESIGN.md §1 이 이 서비스를 "정보
서비스가 아니라 의사결정 지원" 이라 못박았고, 이 구조는 그 정의를 화면 순서로 옮긴 것이다.
P1 은 앞 셋에서, P2 는 둘째~넷째에서, P3 는 마지막에서 자기 것을 찾는다.

### 4-2. 기각

| 안 | 모양 | 기각 이유 |
| --- | --- | --- |
| A. 여행의 하루 | 출발 전 → 아침 → 낮 → 비 오는 오후 → 위급 | 따뜻하지만 길다. 심사자가 기능 범위를 빨리 잡지 못하고, "하루" 프레임에 출발 전 준비(필터·일정)가 잘 안 들어간다 |
| C. 기능 카드 그리드 | 6기능 카드 + 숫자 밴드 | 훑기는 빠르나 템플릿 랜딩으로 읽힌다. 판정과 근거가 다른 카드와 **같은 무게**가 되어 차별점이 사라진다 |

---

## 5. 절별 설계

문구는 **여기서 확정**한다(feature 인덱스 규칙). 전부 해요체, 용어표(DESIGN.md §1) 준수,
"최고의 · 완벽한" 류 금지. 아래 문구는 구현 시 `messages.about` 으로 옮기고, 이미 있는
`footer.tagline` · `footer.sources` · `footer.disclaimer` · `footer.contest` 는 **다시 적지 않고
그대로 읽는다**(about-view 머리 주석의 원칙 유지).

### 5-1. 히어로 (P1)

| 요소 | 내용 |
| --- | --- |
| 표지어 | `반려견과 함께하는 제주 여행` |
| `h1` (보임) | `동반 가능한 곳만 골라서, 지금 가도 되는지까지 봐요` |
| 부제 | `날씨·노면 온도·혼잡도를 공공데이터로 확인하고, 반려견의 크기와 성향에 맞는 장소로 하루 일정을 짜요. 회원가입 없이도 오늘 판정부터 볼 수 있어요.` |
| CTA 1 (채움) | `오늘 판정 보러 가기` → `/` |
| CTA 2 (윤곽) | `장소 찾기` → `/places` |
| 예시 카드 | 아래 |

**예시 카드는 고정값이다.** 백엔드를 부르지 않는다. 내용은 홈 판정 카드와 같은 어휘를 쓰되
값은 코드 상수다.

```text
오늘 산책 · 제주시 기준                    [위험]  ← --metric-critical
기온 29℃ · 추정 노면(아스팔트) 온도 56.0℃ · 체감온도 31℃
· 노면 온도가 52℃ 를 넘어 발바닥 화상 위험이 있어요
· 오늘 산책하기 좋은 시간은 06:00–08:00 이에요
화면 예시예요. 실제 값은 기상청 실시간 관측으로 계산해요.
```

- 등급어 `위험` 은 `--metric-critical-*` 로 칠한다 — 예시라도 **실제 등급 자리**다(§6-2).
- 숫자 `56.0℃` 는 홈 문구 주석의 실제 제보 사례(기온 29℃ · 노면 56℃)에서 가져왔다. "온도가
  잘못됐다" 는 제보가 나온 바로 그 장면이라 **이 서비스의 요점을 한 줄로 보여 준다.**
- 마지막 줄 캡션은 필수다. 빠지면 방문자가 지금 제주 날씨로 읽는다.

### 5-2. 데려가도 돼요? (P1)

| 요소 | 내용 |
| --- | --- |
| `h2` | `데려가도 돼요?` |
| 리드 | `장소마다 갈리는 동반 조건을 내 반려견 기준으로 걸러요.` |
| 항목 | `크기와 체중을 등록하면 그 조건으로 제주의 동반 가능 장소를 걸러요` / `동반 정보가 없는 곳은 '불가'로 단정하지 않고 정보 없음으로 따로 보여 줘요` / `실내인지, 지금 운영 중인지도 함께 봐요` |
| 예시 | 필터 칩(`내 반려견 기준 · 소형 · 7kg`) + 장소 행 3개(이름 · 동반 조건 태그 · 실내/실외) — 그중 하나는 `정보 없음` 점선 태그 |
| 링크 | `장소 찾기` → `/places` |

`정보 없음` 을 예시에 넣는 이유: `UNKNOWN` 을 배제하지 않는 규칙(feature 인덱스 —
`AllowedPetSize#allows`)이 이 서비스의 신뢰 요소라서다. 감추면 "다 안다" 로 읽힌다.

### 5-3. 지금 나가도 돼요? (P1 · P2)

| 요소 | 내용 |
| --- | --- |
| `h2` | `지금 나가도 돼요?` |
| 리드 | `기온이 괜찮아도 아스팔트는 뜨거워요. 발바닥이 닿는 온도로 판정해요.` |
| 항목 | `기상청 실시간 관측으로 노면(아스팔트) 온도를 추정해요` / `체감온도와 함께 안전·주의·위험으로 판정하고 이유를 붙여요` / `하루 중 산책하기 좋은 시간을 골라 줘요` / `기상특보가 발효되면 그것부터 알려요` |
| 예시 | 골든타임 곡선 — 시간축 24칸, 기온·노면 두 선, 추천 구간(06–08)을 면으로. 홈 D4-1-c 와 같은 표현(세로 막대 없음) |
| 링크 | `오늘 판정 보기` → `/` |

곡선의 색: 기온 `--fg-muted`, 노면 `--metric-critical-500`, 추천 구간 면 `--metric-high-100`.
등급 색이 **데이터 자리**에만 있다는 것을 목업에서도 지킨다.

### 5-4. 오늘 어디 가요? (P1 · P2)

| 요소 | 내용 |
| --- | --- |
| `h2` | `오늘 어디 가요?` |
| 리드 | `점수만 주지 않아요. 왜 그런지 이유를 함께 보여 줘요.` |

네 개의 하위 카드(2×2, 모바일 1단).

| 카드 | 제목 | 본문 | 예시 |
| --- | --- | --- | --- |
| a | `여행 적합도` | `날씨·동반 조건·혼잡도를 묶어 등급으로 답하고, 근거를 접어 둬요.` | 배지 `적합도 높음` + 근거 2줄 |
| b | `한산한 날` | `앞으로 30일 혼잡도 예측에서 가장 한산한 날을 골라요.` | 막대 7개, 하나만 `--congestion-best` |
| c | `AI 일정` | `반려견 조건과 여행 기간을 넣으면 하루 단위 초안을 만들어요. 실제 있는 장소만 후보로 써서 없는 곳을 지어내지 않아요.` | 일자 탭 + 항목 3개 + `하루만 다시 짜기` |
| d | `비가 오면 실내로` | `일자별 날씨를 미리 브리핑하고, 비 오는 날은 가까운 실내 대안을 함께 보여 줘요.` | 날씨 아이콘 + 실내 대안 행 |

- 카드 a 의 등급어는 **서버 `name` 과 같은 어휘**(`적합도 높음`)를 쓴다. 숫자 점수는 쓰지 않는다(§3-2).
- 카드 b 의 막대는 `--congestion-bar` / `--congestion-best` 다. 등급 스케일을 쓰지 않는다
  (DESIGN.md §2-3 #603). 목업도 같다.
- 카드 c 의 마지막 문장은 환각 차단을 사용자 언어로 옮긴 것이다. "환각" · "LLM" 은 쓰지 않는다.
- 링크: `AI 일정 만들기` → `/ai-plans/new` (보호 경로 — 미로그인이면 기존 `toLoginHref` 흐름을 탄다).

### 5-5. 위급하면? (P2)

| 요소 | 내용 |
| --- | --- |
| `h2` | `위급하면?` |
| 리드 | `여행 중 가장 급한 질문에는 가장 짧게 답해요.` |
| 항목 | `현재 위치 기준으로 동물병원·동물약국을 거리순으로 찾아요` / `지금 진료 중인지 함께 보여 줘요` / `여행 일정 안에서도 한 번에 들어가요` |
| 예시 | 시설 행 2개(이름 · 거리 · `진료중`) — `진료중` 은 `--status-open-*` |
| 링크 | `가까운 병원·약국` → `/emergency` |

이 절은 짧다. 긴급 화면의 본질이 "빨리" 라 소개도 길면 어긋난다.

### 5-6. 무엇을 보고 판단하나요 (P3)

| 요소 | 내용 |
| --- | --- |
| `h2` | `무엇을 보고 판단하나요` |
| 리드 | `화면의 모든 등급에는 출처와 이유가 붙어요.` |
| 규모 타일 3 | `315곳 · 반려견 동반 가능 장소` / `214곳 · 동물병원·동물약국` / `5곳 · 공공데이터 원천` |
| 원칙 4 | `추천에는 항상 근거를 붙여요` / `공공데이터는 미리 모아 두고, 화면은 그 데이터만 읽어요` / `AI는 실제 있는 장소 목록 안에서만 골라요` / `일정의 저장과 확정은 사용자가 해요. AI는 제안만 해요` |
| 출처 | `footer.sourcesLabel` + `footer.sources` 목록(기존 `<ul>` 구조 유지) |
| 아키텍처 캡션 | `Next.js BFF · Spring MSA 5서비스 · Spring AI + Ollama` — `caption` 등급 한 줄 |

- 규모 숫자는 `display` 900 이되 색은 `--fg` 다 — 중립 수치에 등급 색을 쓰지 않는다(§2-3).
- 숫자의 출처는 `README.md` 의 설계 규모(≈315 · 214)다. **구현 시 실측치로 갱신**하고, 갈리면
  README 도 같이 고친다(§11).
- **개수는 이 타일 세 개에만 있다** (계획 단계에서 수정). §5-2 · §5-5 의 항목 문구에서
  `315곳` · `214곳` 을 걷었다 — `message-tone.test.ts` 가 세운 원칙("서버가 세는 값을 FE 가
  적어 두지 않는다", 홈 응급 배너가 `3곳` 이라 말하는 동안 목록은 0건이던 사례)과, 같은 사실을
  한 화면에서 두 번 말하지 않는 §1 규칙이 같은 방향이다. 타일에는 **기준 시점 캡션**
  `2026년 9월 적재 기준` 을 붙여 이 수가 실시간 값이 아님을 밝힌다. 이 화면은 백엔드를
  부르지 않으므로 실시간으로 셀 수 없고, 규모 자체는 심사자에게 말해야 하는 사실이다 —
  한 곳 · 시점 표기가 그 절충이다.

### 5-7. 알아두실 점

기존 카드 그대로 — `footer.disclaimer` + `footer.contest`. 제목 `알아두실 점` 유지.

### 5-8. 마무리 CTA (P1)

| 요소 | 내용 |
| --- | --- |
| 제목 | `가입 없이 오늘 판정부터 봐요` |
| 본문 | `반려견을 등록하면 판정과 장소 필터가 내 아이 기준으로 바뀌어요.` |
| CTA 1 | `홈으로 가기` → `/` |
| CTA 2 | `반려견 등록하기` → `/pets/new` (보호 경로 — 로그인 흐름을 탄다) |

---

## 6. 시각 톤 — "소개 페이지 표면" 예외

사용자 결정: **브랜드 그린을 넓게 쓰는 마케팅형 룩.** 이것은 DESIGN.md §0·§1 의 두 규칙과
부딪히므로 **예외를 문서로 연다.**

### 6-1. 무엇을 여는가

| 기존 규칙 | 이 화면에서 | 근거 |
| --- | --- | --- |
| 인터페이스 안은 무채색, 채도는 데이터에만 (§1) | 히어로 · 절 구분 밴드 · 마무리 CTA 에 브랜드 그린 **면**을 쓴다 | 브랜드 에셋 규칙 "인터페이스 안은 무채색, **밖**은 브랜드 컬러". 소개 페이지는 제품을 쓰는 화면이 아니라 **제품을 소개하는 화면**이라 락업의 "스플래시·외부 노출" 자리와 같은 쪽이다 |
| 헤더 심볼 24px, 로고 자리에만 (§1) | 히어로에 락업(심볼 + 워드마크)을 `(auth)` 셸 크기(48 / 40×148)로 둔다 | 히어로에는 데이터가 없다 — §1 이 `(auth)` 셸에 2배를 허용한 근거와 같다. 헤더의 24px 은 그대로다 |
| 마케팅 톤 금지 (§1) | **열지 않는다.** 룩만 열고 카피는 그대로다 | 과장은 데이터 신뢰형 컨셉을 스스로 깎는다. 심사자가 가장 먼저 보는 것이 이 어긋남이다 |

### 6-2. 지키는 것

- **등급 색(`--metric-*`)은 예시 카드의 실제 등급 자리에만.** 절 제목 · 아이콘 · 장식에 쓰지 않는다.
- **혼잡도 막대는 `--congestion-*`**, 영업 상태는 `--status-open-*`. 목업도 제품과 같은 토큰을 쓴다.
- **브랜드 그린 위에 글자를 얹는 면은 `--brand-600` 이상**(흰 글자 5.27:1 · 700 은 6.91:1). `--brand-500` 위에 글자 금지는 그대로다(§2-4).
- **새 색을 만들지 않는다.** 연한 그린 두 단은 팔레트에 이미 있는 값을 **이름만 새로** 부른다.

| 새 토큰 | 값 | 같은 값의 기존 토큰 | 용도 |
| --- | --- | --- | --- |
| `--intro-band` | `#F0F7F3` | `--row-selected` | 절 배경 밴드 |
| `--intro-tint` | `#E4F0EA` | `--metric-high-100` | 규모 타일 · 아이콘 배경 |

값이 같아도 이름을 분리하는 것은 §2-3 #598 의 원칙(다른 축은 자기 토큰을 낸다)이다.
`--metric-high-100` 을 절 배경에 그대로 쓰면 "적합도 높음" 의 tint 가 브랜드 면이 된다.

**`--brand-50` · `--brand-100` 이라는 이름은 쓰지 않는다** (계획 단계에서 수정). 그 이름은
§2-7 이 **폐기한 토큰**이고 `src/styles/token-usage.test.ts` 의 `RETIRED` 목록이 소스에서
되살아나지 못하게 막는다. 폐기 이유("아트보드 사용 0회")는 지금도 그대로라 되살리지 않고,
이 축의 이름(`intro`)을 새로 낸다 — 다른 화면이 "브랜드 tint" 로 읽어 가져다 쓰는 것도 막는다.

- **그린 면 위 버튼은 `Button` 의 변형으로 낸다** — `inverse`(흰 면 + `--brand-700` 글자,
  6.91:1) · `inverseOutline`(투명 면 + 흰 글자 + 흰 55% 테두리). `className` 으로 외형을 덮지
  않는다는 `component-guide.md` §3 규칙 그대로다. 두 변형의 사용처는 이 화면의 그린 밴드뿐이다.

- **타이포 스케일 7단 안에서만.** 히어로 `h1` 은 `page`(34/44/800), 절 `h2` 는 `display`(28/36/800), 카드 제목 `title-2`, 본문 `body-2`, 캡션 `caption`. 스케일 밖 값을 만들지 않는다.
- **spacing · radius · shadow 는 기존 스케일.** 카드는 radius 12 + 1px 테두리, **그림자 없음 — 히어로 예시 카드도 같다** (계획 단계에서 수정). 처음 안은 히어로 카드만 `--shadow-md` 였는데, `token-usage.test.ts` 가 그림자를 허용 파일 목록(`FLOATING`)으로 잠그고 있고, 그린 면 위의 흰 카드는 그림자 없이도 충분히 갈린다. 예외 목록에 소개 화면을 더하는 것보다 규칙을 그대로 두는 쪽이 싸다.
- **모바일 <768 에서 카드는 전폭**(§0). 그린 밴드는 원래 전폭이다.

### 6-3. DESIGN.md 갱신

§0 뒤에 **"0-2. 소개 페이지 표면"** 절을 추가한다. 내용은 §6-1 표와 §6-2 토큰 표. 이 절이
없으면 다음 화면이 "소개도 그린을 썼으니" 로 예외를 넘겨받는다 — **적용 범위를 `/about`
하나로 명시한다.**

### 6-4. 모션 — "살아 있는 서비스" 를 예시가 스스로 보여 준다

사용자 결정(2026-09-15): **스크롤 등장(1단) + 예시 자기 재생(2단).** 만져 보는 예시(3단 —
필터 칩 전환 · 곡선 문지르기)는 범위 밖이고, 기온 슬라이더로 노면 온도가 바뀌는 안은
**기각**했다 — 노면 추정식은 서버 것이라 FE 가 흉내 내면 가짜 계산을 보여 주는 셈이고,
"채도는 데이터에만" 과 같은 축에서 신뢰 요소를 스스로 깎는다.

**1단 · 스크롤 등장.**

| 항목 | 값 |
| --- | --- |
| 대상 | 절(§5-2~5-8)의 카드 · `points` 항목 · 규모 타일 · 원칙 행. **히어로는 제외** — 첫 화면은 바로 보인다 |
| 효과 | 투명도 0→1 + `translateY(12px)`→0, **200ms ease-out**(§8 변형 값), 형제 사이 **60ms** 지연 |
| 트리거 | `IntersectionObserver`, 뷰포트 하단에서 10% 들어올 때, **1회만** |
| JS 없음 | **처음 렌더는 보이는 상태다.** 마운트 뒤 아직 화면 밖인 요소만 숨겨 두고 등장시킨다. 로드 시 이미 보이는 요소는 애니메이션하지 않는다 — 검색 봇 · JS 실패 · 느린 기기에서 빈 화면이 생기지 않는다 |
| 구현 | 클라이언트 컴포넌트 **`Reveal` 하나**가 자식을 감싼다. 절 본문은 서버 컴포넌트로 남는다 |

**2단 · 예시 자기 재생.** 화면에 들어올 때 **1회** 재생하고 끝 상태에 머문다.

| 예시 | 재생 | 길이 | 구현 |
| --- | --- | --- | --- |
| 히어로 판정 카드 | 기온 · 노면 · 체감 숫자가 0 → 실제 값으로 카운트업, 끝난 뒤 `위험` 배지 등장 | 600ms + 150ms | 클라이언트 `VerdictSpecimen`. 히어로는 로드 시 보이므로 **마운트 직후** 재생 |
| 골든타임 곡선 | 두 선이 왼쪽→오른쪽으로 그려짐(`stroke-dasharray/offset`), 추천 구간 면이 뒤따라 등장 | 800ms + 200ms | 클라이언트 `GoldenCurveSpecimen`, 뷰포트 진입 시 |
| 혼잡도 막대 | 바닥에서 자라남, 형제 60ms 지연, **목요일 막대만 마지막에** `--congestion-best` 로 | 400ms | 클라이언트 `CongestionSpecimen`, 뷰포트 진입 시 |
| AI 일정 카드 | 1·2·3일차 탭이 **실제로 눌린다** — 고정 예시 항목 3세트 전환. **자동 순환 없음** | 150ms(색) | 클라이언트 `PlanSpecimen`, 로컬 상태 하나 |

**공통 규칙.**

- **자동 재생은 1회. 무한 반복 없음.** 살아 있다는 인상은 "한 번 그려진다" 에서 나오고, 계속
  움직이는 것은 데이터 화면에서 소음이다(§1 "반복은 강조가 아니라 소음").
- **끝 상태가 정본이다.** `prefers-reduced-motion: reduce` 면 전역 규칙(§8 — 한 프레임으로
  줄임)이 그대로 걸려 즉시 끝 상태가 된다. `animation: none` 으로 지우지 않는다.
- **스크린리더는 최종값만 읽는다.** 카운트업 중간값은 `aria-hidden` 인 시각 노드에만 있고,
  실제 값은 처음부터 DOM 에 있다(`sr-only` 또는 `aria-label`). 등급어 `위험` 도 같다.
- **등급 색은 애니메이션의 끝에서만 붙는다.** 재생 중 다른 등급 색을 거치지 않는다 — 배지가
  `안전 → 주의 → 위험` 으로 바뀌는 연출은 하지 않는다. 사실이 아닌 값을 잠깐이라도 보이면
  등급이 장식이 된다.
- **새 duration 은 둘이다** — 600(카운트업) · 800(곡선 그리기). §8 에 "소개 페이지 예시 재생"
  으로 추가하고, 다른 화면은 쓰지 않는다. 등장(200) · 막대(400 = 200×2) · 색(150) 은 기존 값.
- **레이아웃을 움직이지 않는다.** `transform` · `opacity` · SVG `stroke-*` · `height`(막대,
  고정 높이 트랙 안)만 애니메이션한다. 등장 중 문서 높이가 바뀌지 않는다 — 스크롤 점프가
  없어야 IntersectionObserver 트리거가 연쇄로 어긋나지 않는다.
- **클라이언트 컴포넌트는 다섯 개다** — `Reveal` + 예시 4개. 그 밖의 절 본문 · 문구 · 링크는
  서버 컴포넌트다. `fe-boundary-guard` 기준으로 경계는 예시 카드 단위로 닫힌다.

---

## 7. 레이아웃 · 반응형

| 폭 | 히어로 | 절 본문 | 하위 카드 |
| --- | --- | --- | --- |
| 375 (기준) | 1단. 카피 → CTA → 예시 카드 | 1단, 좌우 16 | 1단 |
| 768~1023 | 1단 유지, 예시 카드 폭 512 중앙 | 1단, 좌우 24 | 2열 |
| 1024~ | **2열** — 좌 카피 · 우 예시 카드(400) | 콘텐츠 폭 **1152**(`max-w-6xl`) 중앙 | 2열 |
| 1440~ | 그린 밴드는 전폭, 안쪽 내용은 1152 캡 | 같음 | 같음 |

- 콘텐츠 폭 1152 는 `--content-max` 1440 안이다. 읽는 화면이라 `max-w-screen-md`(768)보다
  넓게 두되 1440 을 다 쓰지 않는다 — 한 줄이 너무 길어진다. **새 레이아웃 토큰은 만들지
  않는다**: Tailwind 스케일의 `max-w-6xl`(72rem = 1152) 하나로 끝난다. 목업의 1120 은
  스케일 밖 값이라 계획 단계에서 1152 로 맞췄다 — arbitrary value(`max-w-[1120px]`)는
  이 저장소가 금지한다(§4).
- 그린 밴드가 전폭이어야 하므로 밴드는 **`Canvas` 층**에 둔다(§0 "바닥은 전폭, 쌓기는 폭
  안"). `Canvas` 는 `--bg-sunken` 바닥을 칠하고, 그 안에 전폭 `<section>` 밴드가 자기 배경
  (`--intro-band` · `--brand-700`)을 갖는다. 밴드 안쪽 내용만 1152 다. 이 화면은
  `SurfaceStack` 을 쓰지 않는다 — 밴드가 그 역할(세로 쌓기 · 간격)을 맡는다.
- 헤더 · 푸터 · 탭바는 `AppShell` 그대로. 소개 페이지가 셸을 바꾸지 않는다.
- 375 에서 가로 스크롤은 버그다. 곡선 · 막대 예시는 자기 폭 안에서 그린다.

---

## 8. 접근성

- **`h1` 은 보인다.** 지금의 `sr-only` `h1` 을 걷는다 — 히어로가 곧 화면 제목이다.
  `h2` 상향은 `h1` 이 보이는 화면 규칙(`lg:`)을 따른다(§3-1 #358).
- 랜드마크는 셸의 것(`header` · `nav`×2 · `main` · `footer`)만. 절은 `section` +
  `aria-labelledby`.
- 등급 · 상태는 **글자가 먼저** — `위험` · `적합도 높음` · `진료중` 을 색 없이도 읽는다.
- 예시 그래프(곡선 · 막대)는 `role="img"` + `aria-label` 한 문장. 값이 데이터가 아니라 예시라
  세부 표를 붙이지 않는다.
- 그린 면 위 글자는 흰색, 면은 `--brand-700`(6.91:1). 링크는 `--link` 그대로.
- 터치 영역 44×44. CTA 버튼 높이 44 이상.

---

## 9. 구현 메모

이 절은 구현 계획의 입력이다. 구현 자체는 `writing-plans` 뒤에 한다.

- **페이지 · 절 본문은 서버 컴포넌트 · 프리페치 없음 · 세션 조회 없음** — 지금과 같다.
  클라이언트 경계는 §6-4 의 다섯 컴포넌트(`Reveal` + 예시 4개)로 닫힌다. 예시 값은
  `features/about/` 안의 상수 파일 하나에 둔다. `types/`·`mocks/` 에 넣지 않는다 — 계약이
  아니라 그림이다.
- `Reveal` 은 `components/infinite-scroll-sentinel.tsx` 의 `IntersectionObserver` 패턴을 따른다
  (cleanup 에서 `disconnect`). 예시 컴포넌트는 `useEffect` 로 1회 재생 플래그만 세우고, 실제
  애니메이션은 CSS(`@keyframes` · `transition`)가 한다 — rAF 루프로 숫자를 직접 그리지 않는다.
  카운트업만 예외로 rAF 를 쓰되 600ms 뒤 반드시 끝 값으로 고정한다(브라우저 패널이 숨겨지면
  rAF 가 멈춘다 — 끝 값 고정이 없으면 중간값에 머문다).
- `messages.about` 에 §5 문구를 추가한다. `footer.*` 는 재사용. `message-tone.test.ts` 가 해요체를 감시한다.
- 보호 경로 링크(`/ai-plans/new` · `/pets/new`)는 **그 경로로 바로 건다** (계획 단계에서 수정).
  `proxy.ts` 가 `PROTECTED_PATHS` 를 보고 미로그인이면 `/login?returnTo=<경로>` 로 보내므로
  `toLoginHref` 를 여기서 부를 필요가 없다 — 부르려면 세션을 읽어야 하고 이 화면은 세션을
  읽지 않는다. 링크가 실제로 가는 곳인지는 `site-footer.test.ts` 의 "갈 곳 있는 링크만"
  패턴으로 잠근다.
- 그린 밴드는 `Canvas` 에 배경을 바꿀 수 있는 최소 변경으로 — `Canvas` 에 톤 prop 을 여는지, 소개 전용 래퍼를 두는지는 구현 계획에서 정한다. **`Surface` 안에서 배경을 덮지 않는다**(§0 "면은 카드가 소유한다").
- 골든타임 곡선 · 혼잡 막대 예시는 홈 · 장소 상세의 실제 컴포넌트를 **재사용하지 않는다** — 그 컴포넌트는 서버 타입에 묶여 있고, 여기는 고정 그림이다. 순수 SVG 로 그린다.
- 이미지는 없다. 사진을 넣으면 출처 표기가 또 필요하고(`사진 출처: 한국관광공사`), 소개가 갤러리로 읽힌다.

---

## 10. 검증

| 항목 | 방법 |
| --- | --- |
| 375 가로 스크롤 없음 | 브라우저 375 · `document.documentElement.scrollWidth === 375` |
| 그린 면 위 글자 대비 | `--brand-700` 흰 글자 6.91:1 — `contrast.test.ts` 에 새 토큰 쌍 추가 |
| 등급 색이 데이터 자리에만 | 렌더 문자열에서 `metric-` 클래스가 예시 카드 · 곡선 · 적합도 배지 밖에 없음 |
| 해요체 · 금지어 | `message-tone.test.ts` · 금지어 grep 0건 — **홍보성 용례**(`최고의`·`완벽한`·`강아지`·`애견`·`펫 프렌들리`). `최고 체감온도` 처럼 **기상 용어의 "최고"** 는 금지어가 아니다 — 홈 문구가 이미 `최고 체감온도` 를 쓴다(구현 중 판정, Task 2 검토) |
| 링크가 전부 실제 라우트 | `href` 집합 ⊆ {`/`, `/places`, `/ai-plans/new`, `/emergency`, `/pets/new`} |
| 새 색 0개 | `globals.css` 의 새 값이 기존 값 집합에 포함 |
| JS 없이 내용이 보임 | `renderToStaticMarkup` 결과에 숨김 클래스(`opacity-0` 류)가 **없음** — 숨김은 마운트 뒤에만 |
| 최종값이 DOM 에 처음부터 있음 | 정적 마크업에 `29` · `56.0` · `31` · `위험` 이 포함 |
| 1회 재생 | 뷰포트를 벗어나 다시 들어와도 재생 플래그가 유지됨 (브라우저 확인) |
| reduced-motion | `prefers-reduced-motion: reduce` 에뮬레이션에서 로드 직후 끝 상태 (브라우저 확인) |
| 등장 중 문서 높이 불변 | `scrollHeight` 가 등장 전후 같음 (브라우저 확인) |
| `pnpm verify` | 저장소 표준 |

---

## 11. 같이 움직이는 문서

| 문서 | 변경 |
| --- | --- |
| `frontend/DESIGN.md` | §0-2 "소개 페이지 표면" 추가(토큰 `--intro-band/tint` · `Button` 변형 `inverse/inverseOutline` · 락업 크기 · 적용 범위 `/about`), §2-1 에 두 토큰 포인터, §8 에 "소개 페이지 예시 재생 600/800ms" 추가 |
| `frontend/src/styles/tokens.css` · `app/globals.css` | `--intro-band` · `--intro-tint` 선언과 `@theme` 매핑 |
| `frontend/src/components/button.tsx` | `inverse` · `inverseOutline` 변형 |
| `frontend/docs/features/about/` | 세부명세(D0~D8) 신규 — `fe-spec-writer` 가 이 문서를 옮긴다 |
| `frontend/docs/features/_index.md` | `about` 행 추가 (지금은 `brand` 옆에 없다) |
| `frontend/docs/screen-inventory.md` | 소개 화면 행 |
| `frontend/src/lib/messages/about.ts` | 머리 주석의 "출처 표기용" 서술 갱신 |
| `README.md` | 규모 숫자가 실측과 갈리면 함께 갱신 |

---

## 12. 후속 · 미결

- 이슈는 #635 로 발행했고 브랜치는 `feature/fe/635-about-landing` 이다.
- 규모 숫자(315 · 214)는 구현 시 dev 게이트웨이에서 실측한다. 갈리면 실측이 이긴다.
- OG 이미지 · 공유 카드 문구는 `brand` 명세 몫이다. 소개 페이지 전용 `og:description` 을 둘지는
  구현 계획에서 정한다 — 추천은 `messages.about.description` 재사용.
- 영문 소개는 범위 밖이다. 공모전 출품과 실사용자 모두 한국어다.
